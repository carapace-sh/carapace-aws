name: update-domain
description: Updates a Amazon DataZone domain.
flags:
    --cli-input-json=: Read arguments from the JSON string provided
    --cli-input-yaml=: Read arguments from the YAML string provided
    --client-token=: A unique, case-sensitive identifier that is provided to ensure the idempotency of the request.
    --description=: The description to be updated as part of the `UpdateDomain` action.
    --domain-execution-role=: The domain execution role to be updated as part of the `UpdateDomain` action.
    --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request
    --identifier=!: The ID of the Amazon Web Services domain that is to be updated.
    --name=: The name to be updated as part of the `UpdateDomain` action.
    --service-role=: The service role of the domain.
    --single-sign-on=: The single sign-on option to be updated as part of the `UpdateDomain` action.
