name: create-core-network-prefix-list-association
description: Creates an association between a core network and a prefix list for routing control.
flags:
    --cli-input-json=: Read arguments from the JSON string provided.
    --cli-input-yaml=: Read arguments from the YAML string provided.
    --client-token=: A unique, case-sensitive identifier that you provide to ensure the idempotency of the request.
    --core-network-id=!: The ID of the core network to associate with the prefix list.
    --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
    --prefix-list-alias=!: An optional alias for the prefix list association.
    --prefix-list-arn=!: The ARN of the prefix list to associate with the core network.
