name: list-test-grid-session-artifacts
description: Retrieves a list of artifacts created during the session.
flags:
    --max-result=: The maximum number of results to be returned by a request.
    --next-token=: Pagination token.
    --session-arn=!: The ARN of a [TestGridSession]().
    --type=: Limit results to a specified type of artifact.
completion:
    flag:
        type:
            - VIDEO
            - LOG
