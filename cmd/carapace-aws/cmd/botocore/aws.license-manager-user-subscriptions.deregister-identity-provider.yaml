name: deregister-identity-provider
description: Deregisters the Active Directory identity provider from License Manager user-based subscriptions.
flags:
    --identity-provider-arn=: The Amazon Resource Name (ARN) that identifies the identity provider to deregister.
    --identity-provider=: An object that specifies details for the Active Directory identity provider.
    --product=: The name of the user-based subscription product.
