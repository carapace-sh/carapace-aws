name: list-publishing-destinations
description: Returns a list of publishing destinations associated with the specified `detectorId`.
flags:
    --detector-id=!: The detector ID for which you want to retrieve the publishing destination.
    --max-results=: The maximum number of results to return in the response.
    --next-token=: A token to use for paginating results that are returned in the response.
