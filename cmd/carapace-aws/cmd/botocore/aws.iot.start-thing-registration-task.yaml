name: start-thing-registration-task
description: Creates a bulk thing provisioning task.
flags:
    --input-file-bucket=!: The S3 bucket that contains the input file.
    --input-file-key=!: The name of input file within the S3 bucket.
    --role-arn=!: The IAM role ARN that grants permission the input file.
    --template-body=!: The provisioning template.
