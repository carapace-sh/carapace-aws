name: delete-connection
description: Deletes and connection.
flags:
    --domain-identifier=!: The ID of the domain where the connection is deleted.
    --identifier=!: The ID of the connection that is deleted.
