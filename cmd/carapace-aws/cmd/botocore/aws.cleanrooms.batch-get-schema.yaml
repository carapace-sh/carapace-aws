name: batch-get-schema
description: Retrieves multiple schemas by their identifiers.
flags:
    --collaboration-identifier=!: A unique identifier for the collaboration that the schemas belong to.
    --names=!: The names for the schema objects to retrieve.
