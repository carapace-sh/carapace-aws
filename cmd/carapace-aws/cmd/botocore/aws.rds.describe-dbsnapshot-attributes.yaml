name: describe-dbsnapshot-attributes
description: Returns a list of DB snapshot attribute names and values for a manual DB snapshot.
flags:
    --dbsnapshot-identifier=!: The identifier for the DB snapshot to describe the attributes for.
