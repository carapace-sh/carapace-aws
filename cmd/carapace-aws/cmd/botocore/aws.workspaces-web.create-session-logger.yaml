name: create-session-logger
description: Creates a session logger.
flags:
    --additional-encryption-context=: The additional encryption context of the session logger.
    --client-token=: A unique, case-sensitive identifier that you provide to ensure the idempotency of the request.
    --customer-managed-key=: The custom managed key of the session logger.
    --display-name=: The human-readable display name for the session logger resource.
    --event-filter=!: The filter that specifies the events to monitor.
    --log-configuration=!: The configuration that specifies where logs are delivered.
    --tags=: The tags to add to the session logger.
