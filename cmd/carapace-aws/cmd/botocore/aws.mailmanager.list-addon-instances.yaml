name: list-addon-instances
description: Lists all Add On instances in your account.
flags:
    --next-token=: If you received a pagination token from a previous call to this API, you can provide it here to continue paginating through the next page of results.
    --page-size=: The maximum number of ingress endpoint resources that are returned per call.
