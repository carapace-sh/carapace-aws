name: untag-resource
description: Removes metadata tags from a FinSpace resource.
flags:
    --resource-arn=!: A FinSpace resource from which you want to remove a tag or tags.
    --tag-keys=!: The tag keys (names) of one or more tags to be removed.
