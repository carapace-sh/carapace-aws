name: describe-dbcluster-snapshot-attributes
description: Returns a list of DB cluster snapshot attribute names and values for a manual DB cluster snapshot.
flags:
    --dbcluster-snapshot-identifier=!: The identifier for the DB cluster snapshot to describe the attributes for.
