name: update-configured-table-association
description: Updates a configured table association.
flags:
    --cli-input-json=: Read arguments from the JSON string provided
    --cli-input-yaml=: Read arguments from the YAML string provided
    --configured-table-association-identifier=!: The unique identifier for the configured table association to update.
    --description=: A new description for the configured table association.
    --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request
    --membership-identifier=!: The unique ID for the membership that the configured table association belongs to.
    --role-arn=: The service will assume this role to access catalog metadata and query the table.
