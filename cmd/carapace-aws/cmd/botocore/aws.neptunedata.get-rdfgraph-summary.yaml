name: get-rdfgraph-summary
description: Gets a graph summary for an RDF graph.
flags:
    --mode=: 'Mode can take one of two values: `BASIC` (the default), and `DETAILED`.'
completion:
    flag:
        mode:
            - basic
            - detailed
