name: create-local-gateway-virtual-interface
description: Create a virtual interface for a local gateway.
flags:
    --cli-input-json=: Read arguments from the JSON string provided
    --cli-input-yaml=: Read arguments from the YAML string provided
    --dry-run: Checks whether you have the required permissions for the action, without actually making the request, and provides an error response.
    --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request
    --local-address=!: The IP address assigned to the local gateway virtual interface on the Outpost side.
    --local-gateway-virtual-interface-group-id=!: The ID of the local gateway virtual interface group.
    --no-dry-run: Checks whether you have the required permissions for the action, without actually making the request, and provides an error response.
    --outpost-lag-id=!: References the Link Aggregation Group (LAG) that connects the Outpost to on-premises network devices.
    --peer-address=!: The peer IP address for the local gateway virtual interface.
    --peer-bgp-asn-extended=: The extended 32-bit ASN of the BGP peer for use with larger ASN values.
    --peer-bgp-asn=: The Autonomous System Number (ASN) of the Border Gateway Protocol (BGP) peer.
    --tag-specifications=: The tags to apply to a resource when the local gateway virtual interface is being created.
    --vlan=!: The virtual local area network (VLAN) used for the local gateway virtual interface.
