name: get-decrypted-api-key
description: Returns your API key in decrypted form.
flags:
    --api-key=!: The encrypted API key.
    --scope=!: Specifies whether this is for a global resource type, such as a Amazon CloudFront distribution.
completion:
    flag:
        scope:
            - CLOUDFRONT
            - REGIONAL
