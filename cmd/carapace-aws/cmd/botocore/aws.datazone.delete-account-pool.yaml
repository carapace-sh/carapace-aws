name: delete-account-pool
description: Deletes an account pool.
flags:
    --domain-identifier=!: The ID of the domain where the account pool is deleted.
    --identifier=!: The ID of the account pool to be deleted.
