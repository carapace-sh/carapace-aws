name: list-lineage-events
description: Lists lineage events.
flags:
    --cli-input-json=: Read arguments from the JSON string provided
    --cli-input-yaml=: Read arguments from the YAML string provided
    --domain-identifier=!: The ID of the domain where you want to list lineage events.
    --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request
    --max-results=: The maximum number of lineage events to return in a single call to ListLineageEvents.
    --next-token=: When the number of lineage events is greater than the default value for the MaxResults parameter, or if you explicitly specify a value for MaxResults that is less than the number of lineage events, the response includes a pagination token named NextToken.
    --processing-status=: The processing status of a lineage event.
    --sort-order=: The sort order of the lineage events.
    --timestamp-after=: The after timestamp of a lineage event.
    --timestamp-before=: The before timestamp of a lineage event.
completion:
    flag:
        processing-status:
            - REQUESTED
            - PROCESSING
            - SUCCESS
            - FAILED
        sort-order:
            - ASCENDING
            - DESCENDING
