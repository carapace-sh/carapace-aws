name: list-network-profiles
description: Returns the list of available network profiles.
flags:
    --arn=!: The Amazon Resource Name (ARN) of the project for which you want to list network profiles.
    --cli-input-json=: Read arguments from the JSON string provided
    --cli-input-yaml=: Read arguments from the YAML string provided
    --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request
    --next-token=: An identifier that was returned from the previous call to this operation, which can be used to return the next set of items in the list.
    --type=: The type of network profile to return information about.
completion:
    flag:
        type:
            - CURATED
            - PRIVATE
