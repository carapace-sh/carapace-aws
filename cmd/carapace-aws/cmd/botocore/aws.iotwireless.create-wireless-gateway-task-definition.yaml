name: create-wireless-gateway-task-definition
description: Creates a gateway task definition.
flags:
    --auto-create-tasks=!: Whether to automatically create tasks using this task definition for all gateways with the specified current version.
    --client-request-token=: Each resource must have a unique client request token.
    --name=: The name of the new resource.
    --tags=: The tags to attach to the specified resource.
    --update=: Information about the gateways to update.
