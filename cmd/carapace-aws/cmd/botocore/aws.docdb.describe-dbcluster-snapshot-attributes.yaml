name: describe-dbcluster-snapshot-attributes
description: Returns a list of cluster snapshot attribute names and values for a manual DB cluster snapshot.
flags:
    --dbcluster-snapshot-identifier=!: The identifier for the cluster snapshot to describe the attributes for.
