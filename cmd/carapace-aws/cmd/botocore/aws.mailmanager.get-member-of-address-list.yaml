name: get-member-of-address-list
description: Fetch attributes of a member in an address list.
flags:
    --address-list-id=!: The unique identifier of the address list to retrieve the address from.
    --address=!: The address to be retrieved from the address list.
