name: get-rule
description: Gets the details of a rule in Amazon DataZone.
flags:
    --domain-identifier=!: The ID of the domain where the `GetRule` action is to be invoked.
    --identifier=!: The ID of the rule.
    --revision=: The revision of the rule.
