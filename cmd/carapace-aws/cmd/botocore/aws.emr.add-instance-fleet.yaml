name: add-instance-fleet
description: Adds an instance fleet to a running cluster.
flags:
    --cluster-id=!: The unique identifier of the cluster.
    --instance-fleet=!: Specifies the configuration of the instance fleet.
