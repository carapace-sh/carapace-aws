name: list-target-groups
description: Lists your target groups.
flags:
    --max-results=: The maximum number of results to return.
    --next-token=: A pagination token for the next page of results.
    --target-group-type=: The target group type.
    --vpc-identifier=: The ID or ARN of the VPC.
completion:
    flag:
        target-group-type:
            - IP
            - LAMBDA
            - INSTANCE
            - ALB
