name: enable-organization-admin-account
description: Designates the Security Hub administrator account for an organization.
flags:
    --admin-account-id=!: The Amazon Web Services account identifier of the account to designate as the Security Hub administrator account.
    --cli-input-json=: Read arguments from the JSON string provided
    --cli-input-yaml=: Read arguments from the YAML string provided
    --feature=: The feature for which the delegated admin account is enabled.
    --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request
completion:
    flag:
        feature:
            - SecurityHub
            - SecurityHubV2
