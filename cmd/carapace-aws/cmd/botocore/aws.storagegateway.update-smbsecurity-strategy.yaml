name: update-smbsecurity-strategy
description: Updates the SMB security strategy level for an Amazon S3 file gateway.
flags:
    --gateway-arn=!: ""
    --smbsecurity-strategy=!: Specifies the type of security strategy.
completion:
    flag:
        smbsecurity-strategy:
            - ClientSpecified
            - MandatorySigning
            - MandatoryEncryption
            - MandatoryEncryptionNoAes128
