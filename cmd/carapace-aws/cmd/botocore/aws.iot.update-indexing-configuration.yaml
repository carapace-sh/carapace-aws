name: update-indexing-configuration
description: Updates the search configuration.
flags:
    --thing-group-indexing-configuration=: Thing group indexing configuration.
    --thing-indexing-configuration=: Thing indexing configuration.
