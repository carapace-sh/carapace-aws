name: describe-alias
description: |-
    **This API works with the following fleet types:** EC2, Anywhere, Container

    Retrieves properties for an alias.
flags:
    --alias-id=!: The unique identifier for the fleet alias that you want to retrieve.
