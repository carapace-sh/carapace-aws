name: update-cluster-settings
description: Modifies the settings to use for a cluster.
flags:
    --cluster=!: The name of the cluster to modify the settings for.
    --settings=!: The setting to use by default for a cluster.
