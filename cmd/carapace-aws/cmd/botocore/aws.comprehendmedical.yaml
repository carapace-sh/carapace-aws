name: comprehendmedical
description: AWS Comprehend Medical
commands:
    - name: describe-entities-detection-v2-job
      description: Gets the properties associated with a medical entities detection job.
      flags:
        --job-id=!: The identifier that Amazon Comprehend Medical generated for the job.
    - name: describe-phi-detection-job
      description: Gets the properties associated with a protected health information (PHI) detection job.
      flags:
        --job-id=!: The identifier that Amazon Comprehend Medical generated for the job.
    - name: describe-snomedct-inference-job
      description: Gets the properties associated with an InferSNOMEDCT job.
      flags:
        --job-id=!: The identifier that Amazon Comprehend Medical generated for the job.
    - name: detect-phi
      description: Inspects the clinical text for protected health information (PHI) entities and returns the entity category, location, and confidence score for each entity.
      flags:
        --text=!: A UTF-8 text string containing the clinical content being examined for PHI entities.
    - name: infer-ic-d10-cm
      description: InferICD10CM detects medical conditions as entities listed in a patient record and links those entities to normalized concept identifiers in the ICD-10-CM knowledge base from the Centers for Disease Control.
      flags:
        --text=!: The input text used for analysis.
    - name: list-rx-norm-inference-jobs
      description: Gets a list of InferRxNorm jobs that you have submitted.
      flags:
        --filter=: Filters the jobs that are returned.
        --max-results=: Identifies the next page of results to return.
        --next-token=: Identifies the next page of results to return.
    - name: start-rx-norm-inference-job
      description: Starts an asynchronous job to detect medication entities and link them to the RxNorm ontology.
      flags:
        --client-request-token=: A unique identifier for the request.
        --data-access-role-arn=!: The Amazon Resource Name (ARN) of the AWS Identity and Access Management (IAM) role that grants Amazon Comprehend Medical read access to your input data.
        --input-data-config=!: Specifies the format and location of the input data for the job.
        --job-name=: The identifier of the job.
        --kms-key=: An AWS Key Management Service key to encrypt your output files.
        --language-code=!: The language of the input documents.
        --output-data-config=!: Specifies where to send the output files.
      completion:
        flag:
            language-code:
                - en
    - name: start-snomedct-inference-job
      description: Starts an asynchronous job to detect medical concepts and link them to the SNOMED-CT ontology.
      flags:
        --client-request-token=: A unique identifier for the request.
        --data-access-role-arn=!: The Amazon Resource Name (ARN) of the AWS Identity and Access Management (IAM) role that grants Amazon Comprehend Medical read access to your input data.
        --input-data-config=!: ""
        --job-name=: The user generated name the asynchronous InferSNOMEDCT job.
        --kms-key=: An AWS Key Management Service key used to encrypt your output files.
        --language-code=!: The language of the input documents.
        --output-data-config=!: ""
      completion:
        flag:
            language-code:
                - en
    - name: detect-entities-v2
      description: Inspects the clinical text for a variety of medical entities and returns specific information about them such as entity category, location, and confidence score on that information.
      flags:
        --text=!: A UTF-8 string containing the clinical content being examined for entities.
    - name: infer-snomedct
      description: InferSNOMEDCT detects possible medical concepts as entities and links them to codes from the Systematized Nomenclature of Medicine, Clinical Terms (SNOMED-CT) ontology
      flags:
        --text=!: The input text to be analyzed using InferSNOMEDCT.
    - name: start-phi-detection-job
      description: Starts an asynchronous job to detect protected health information (PHI).
      flags:
        --client-request-token=: A unique identifier for the request.
        --data-access-role-arn=!: The Amazon Resource Name (ARN) of the AWS Identity and Access Management (IAM) role that grants Amazon Comprehend Medical read access to your input data.
        --input-data-config=!: Specifies the format and location of the input data for the job.
        --job-name=: The identifier of the job.
        --kms-key=: An AWS Key Management Service key to encrypt your output files.
        --language-code=!: The language of the input documents.
        --output-data-config=!: Specifies where to send the output files.
      completion:
        flag:
            language-code:
                - en
    - name: stop-phi-detection-job
      description: Stops a protected health information (PHI) detection job in progress.
      flags:
        --job-id=!: The identifier of the PHI detection job to stop.
    - name: stop-rx-norm-inference-job
      description: Stops an InferRxNorm inference job in progress.
      flags:
        --job-id=!: The identifier of the job.
    - name: stop-snomedct-inference-job
      description: Stops an InferSNOMEDCT inference job in progress.
      flags:
        --job-id=!: The job id of the asynchronous InferSNOMEDCT job to be stopped.
    - name: detect-entities
      description: The `DetectEntities` operation is deprecated.
      flags:
        --text=!: A UTF-8 text string containing the clinical content being examined for entities.
    - name: infer-rx-norm
      description: InferRxNorm detects medications as entities listed in a patient record and links to the normalized concept identifiers in the RxNorm database from the National Library of Medicine.
      flags:
        --text=!: The input text used for analysis.
    - name: list-ic-d10-cm-inference-jobs
      description: Gets a list of InferICD10CM jobs that you have submitted.
      flags:
        --filter=: Filters the jobs that are returned.
        --max-results=: The maximum number of results to return in each page.
        --next-token=: Identifies the next page of results to return.
    - name: start-entities-detection-v2-job
      description: Starts an asynchronous medical entity detection job for a collection of documents.
      flags:
        --client-request-token=: A unique identifier for the request.
        --data-access-role-arn=!: The Amazon Resource Name (ARN) of the AWS Identity and Access Management (IAM) role that grants Amazon Comprehend Medical read access to your input data.
        --input-data-config=!: The input configuration that specifies the format and location of the input data for the job.
        --job-name=: The identifier of the job.
        --kms-key=: An AWS Key Management Service key to encrypt your output files.
        --language-code=!: The language of the input documents.
        --output-data-config=!: The output configuration that specifies where to send the output files.
      completion:
        flag:
            language-code:
                - en
    - name: start-ic-d10-cm-inference-job
      description: Starts an asynchronous job to detect medical conditions and link them to the ICD-10-CM ontology.
      flags:
        --client-request-token=: A unique identifier for the request.
        --data-access-role-arn=!: The Amazon Resource Name (ARN) of the AWS Identity and Access Management (IAM) role that grants Amazon Comprehend Medical read access to your input data.
        --input-data-config=!: Specifies the format and location of the input data for the job.
        --job-name=: The identifier of the job.
        --kms-key=: An AWS Key Management Service key to encrypt your output files.
        --language-code=!: The language of the input documents.
        --output-data-config=!: Specifies where to send the output files.
      completion:
        flag:
            language-code:
                - en
    - name: stop-entities-detection-v2-job
      description: Stops a medical entities detection job in progress.
      flags:
        --job-id=!: The identifier of the medical entities job to stop.
    - name: stop-ic-d10-cm-inference-job
      description: Stops an InferICD10CM inference job in progress.
      flags:
        --job-id=!: The identifier of the job.
    - name: describe-ic-d10-cm-inference-job
      description: Gets the properties associated with an InferICD10CM job.
      flags:
        --job-id=!: The identifier that Amazon Comprehend Medical generated for the job.
    - name: describe-rx-norm-inference-job
      description: Gets the properties associated with an InferRxNorm job.
      flags:
        --job-id=!: The identifier that Amazon Comprehend Medical generated for the job.
    - name: list-entities-detection-v2-jobs
      description: Gets a list of medical entity detection jobs that you have submitted.
      flags:
        --filter=: Filters the jobs that are returned.
        --max-results=: The maximum number of results to return in each page.
        --next-token=: Identifies the next page of results to return.
    - name: list-phi-detection-jobs
      description: Gets a list of protected health information (PHI) detection jobs you have submitted.
      flags:
        --filter=: Filters the jobs that are returned.
        --max-results=: The maximum number of results to return in each page.
        --next-token=: Identifies the next page of results to return.
    - name: list-snomedct-inference-jobs
      description: Gets a list of InferSNOMEDCT jobs a user has submitted.
      flags:
        --filter=: ""
        --max-results=: The maximum number of results to return in each page.
        --next-token=: Identifies the next page of InferSNOMEDCT results to return.
