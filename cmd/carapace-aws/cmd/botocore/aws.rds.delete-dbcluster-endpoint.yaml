name: delete-dbcluster-endpoint
description: Deletes a custom endpoint and removes it from an Amazon Aurora DB cluster.
flags:
    --dbcluster-endpoint-identifier=!: The identifier associated with the custom endpoint.
