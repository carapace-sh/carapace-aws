name: delete-rule
description: Deletes a rule in Amazon DataZone.
flags:
    --domain-identifier=!: The ID of the domain that where the rule is to be deleted.
    --identifier=!: The ID of the rule that is to be deleted.
