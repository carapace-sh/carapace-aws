name: get-time-series-data-point
description: Gets the existing data point for the asset.
flags:
    --cli-input-json=: Read arguments from the JSON string provided
    --cli-input-yaml=: Read arguments from the YAML string provided
    --domain-identifier=!: The ID of the Amazon DataZone domain that houses the asset for which you want to get the data point.
    --entity-identifier=!: The ID of the asset for which you want to get the data point.
    --entity-type=!: The type of the asset for which you want to get the data point.
    --form-name=!: The name of the time series form that houses the data point that you want to get.
    --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request
    --identifier=!: The ID of the data point that you want to get.
completion:
    flag:
        entity-type:
            - ASSET
            - LISTING
