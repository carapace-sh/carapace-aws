name: list-auto-ml-jobs
description: Request a list of jobs.
flags:
    --cli-input-json=: Read arguments from the JSON string provided.
    --cli-input-yaml=: Read arguments from the YAML string provided.
    --creation-time-after=: Request a list of jobs, using a filter for time.
    --creation-time-before=: Request a list of jobs, using a filter for time.
    --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
    --last-modified-time-after=: Request a list of jobs, using a filter for time.
    --last-modified-time-before=: Request a list of jobs, using a filter for time.
    --max-items=: The  total number of items to return in the command's output.
    --max-results=: Request a list of jobs up to a specified limit.
    --name-contains=: Request a list of jobs, using a search filter for name.
    --next-token=: If the previous response was truncated, you receive this token.
    --page-size=: The size of each page to get in the AWS service call.
    --sort-by=: The parameter by which to sort the results.
    --sort-order=: The sort order for the results.
    --starting-token=: A token to specify where to start paginating.
    --status-equals=: Request a list of jobs, using a filter for status.
completion:
    flag:
        sort-by:
            - Name
            - CreationTime
            - Status
        sort-order:
            - Ascending
            - Descending
        status-equals:
            - Completed
            - InProgress
            - Failed
            - Stopped
            - Stopping
