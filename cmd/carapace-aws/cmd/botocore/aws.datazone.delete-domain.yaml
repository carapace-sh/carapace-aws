name: delete-domain
description: Deletes a Amazon DataZone domain.
flags:
    --cli-input-json=: Read arguments from the JSON string provided.
    --cli-input-yaml=: Read arguments from the YAML string provided.
    --client-token=: A unique, case-sensitive identifier that is provided to ensure the idempotency of the request.
    --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
    --identifier=!: The identifier of the Amazon Web Services domain that is to be deleted.
    --no-skip-deletion-check: Specifies the optional flag to delete all child entities within the domain.
    --skip-deletion-check: Specifies the optional flag to delete all child entities within the domain.
