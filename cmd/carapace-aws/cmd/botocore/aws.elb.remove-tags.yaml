name: remove-tags
description: Removes one or more tags from the specified load balancer.
flags:
    --load-balancer-names=!: The name of the load balancer.
    --tags=!: The list of tag keys to remove.
