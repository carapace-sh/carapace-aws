name: allocate-public-virtual-interface
description: Provisions a public virtual interface to be owned by the specified Amazon Web Services account.
flags:
    --connection-id=!: The ID of the connection on which the public virtual interface is provisioned.
    --new-public-virtual-interface-allocation=!: Information about the public virtual interface.
    --owner-account=!: The ID of the Amazon Web Services account that owns the public virtual interface.
