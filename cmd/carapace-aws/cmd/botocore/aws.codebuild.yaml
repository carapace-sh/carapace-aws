# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: codebuild
description: AWS CodeBuild
commands:
    - name: batch-delete-builds
      description: Deletes one or more builds.
      flags:
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
        --ids=!:
            description: The IDs of the builds to delete.
            nargs: -1
    - name: batch-get-build-batches
      description: Retrieves information about one or more batch builds.
      flags:
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
        --ids=!:
            description: An array that contains the batch build identifiers to retrieve.
            nargs: -1
    - name: batch-get-builds
      description: Gets information about one or more builds.
      flags:
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
        --ids=!:
            description: The IDs of the builds.
            nargs: -1
    - name: batch-get-command-executions
      description: Gets information about the command executions.
      flags:
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --command-execution-ids=!:
            description: A comma separated list of `commandExecutionIds`.
            nargs: -1
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
        --sandbox-id=!: A `sandboxId` or `sandboxArn`.
    - name: batch-get-fleets
      description: Gets information about one or more compute fleets.
      flags:
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
        --names=!:
            description: The names or ARNs of the compute fleets.
            nargs: -1
    - name: batch-get-projects
      description: Gets information about one or more build projects.
      flags:
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
        --names=!:
            description: The names or ARNs of the build projects.
            nargs: -1
    - name: batch-get-report-groups
      description: Returns an array of report groups.
      flags:
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
        --report-group-arns=!:
            description: An array of report group ARNs that identify the report groups to return.
            nargs: -1
    - name: batch-get-reports
      description: Returns an array of reports.
      flags:
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
        --report-arns=!:
            description: An array of ARNs that identify the `Report` objects to return.
            nargs: -1
    - name: batch-get-sandboxes
      description: Gets information about the sandbox status.
      flags:
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
        --ids=!:
            description: A comma separated list of `sandboxIds` or `sandboxArns`.
            nargs: -1
    - name: create-fleet
      description: Creates a compute fleet.
      flags:
        --base-capacity=!: The initial number of machines allocated to the ﬂeet, which deﬁnes the number of builds that can run in parallel.
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --compute-configuration=: The compute configuration of the compute fleet.
        --compute-type=!: Information about the compute resources the compute fleet uses.
        --environment-type=!: The environment type of the compute fleet.
        --fleet-service-role=: The service role associated with the compute fleet.
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
        --image-id=: The Amazon Machine Image (AMI) of the compute fleet.
        --name=!: The name of the compute fleet.
        --overflow-behavior=: The compute fleet overflow behavior.
        --proxy-configuration=: The proxy configuration of the compute fleet.
        --scaling-configuration=: The scaling configuration of the compute fleet.
        --tags=:
            description: A list of tag key and value pairs associated with this compute fleet.
            nargs: -1
        --vpc-config=: ""
      completion:
        flag:
            compute-type:
                - BUILD_GENERAL1_SMALL
                - BUILD_GENERAL1_MEDIUM
                - BUILD_GENERAL1_LARGE
                - BUILD_GENERAL1_XLARGE
                - BUILD_GENERAL1_2XLARGE
                - BUILD_LAMBDA_1GB
                - BUILD_LAMBDA_2GB
                - BUILD_LAMBDA_4GB
                - BUILD_LAMBDA_8GB
                - BUILD_LAMBDA_10GB
                - ATTRIBUTE_BASED_COMPUTE
                - CUSTOM_INSTANCE_TYPE
            environment-type:
                - WINDOWS_CONTAINER
                - LINUX_CONTAINER
                - LINUX_GPU_CONTAINER
                - ARM_CONTAINER
                - WINDOWS_SERVER_2019_CONTAINER
                - WINDOWS_SERVER_2022_CONTAINER
                - LINUX_LAMBDA_CONTAINER
                - ARM_LAMBDA_CONTAINER
                - LINUX_EC2
                - ARM_EC2
                - WINDOWS_EC2
                - MAC_ARM
            overflow-behavior:
                - QUEUE
                - ON_DEMAND
    - name: create-project
      description: Creates a build project.
      flags:
        --artifacts=!: Information about the build output artifacts for the build project.
        --auto-retry-limit=: The maximum number of additional automatic retries after a failed build.
        --badge-enabled: Set this to true to generate a publicly accessible URL for your project's build badge.
        --build-batch-config=: A [ProjectBuildBatchConfig]() object that defines the batch build options for the project.
        --cache=: Stores recently used information so that it can be quickly accessed at a later time.
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --concurrent-build-limit=: The maximum number of concurrent builds that are allowed for this project.
        --description=: A description that makes the build project easy to identify.
        --encryption-key=: The Key Management Service customer master key (CMK) to be used for encrypting the build output artifacts.
        --environment=!: Information about the build environment for the build project.
        --file-system-locations=:
            description: An array of `ProjectFileSystemLocation` objects for a CodeBuild build project.
            nargs: -1
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
        --logs-config=: Information about logs for the build project.
        --name=!: The name of the build project.
        --no-badge-enabled: Set this to true to generate a publicly accessible URL for your project's build badge.
        --queued-timeout-in-minutes=: The number of minutes a build is allowed to be queued before it times out.
        --secondary-artifacts=:
            description: An array of `ProjectArtifacts` objects.
            nargs: -1
        --secondary-source-versions=:
            description: An array of `ProjectSourceVersion` objects.
            nargs: -1
        --secondary-sources=:
            description: An array of `ProjectSource` objects.
            nargs: -1
        --service-role=!: The ARN of the IAM role that enables CodeBuild to interact with dependent Amazon Web Services services on behalf of the Amazon Web Services account.
        --source-version=: A version of the build input to be built for this project.
        --source=!: Information about the build input source code for the build project.
        --tags=:
            description: A list of tag key and value pairs associated with this build project.
            nargs: -1
        --timeout-in-minutes=: How long, in minutes, from 5 to 2160 (36 hours), for CodeBuild to wait before it times out any build that has not been marked as completed.
        --vpc-config=: VpcConfig enables CodeBuild to access resources in an Amazon VPC.
    - name: create-report-group
      description: Creates a report group.
      flags:
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --export-config=!: A `ReportExportConfig` object that contains information about where the report group test results are exported.
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
        --name=!: The name of the report group.
        --tags=:
            description: A list of tag key and value pairs associated with this report group.
            nargs: -1
        --type=!: The type of report group.
      completion:
        flag:
            type:
                - TEST
                - CODE_COVERAGE
    - name: create-webhook
      description: For an existing CodeBuild build project that has its source code stored in a GitHub or Bitbucket repository, enables CodeBuild to start rebuilding the source code every time a code change is pushed to the repository.
      flags:
        --branch-filter=: A regular expression used to determine which repository branches are built when a webhook is triggered.
        --build-type=: Specifies the type of build this webhook will trigger.
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --filter-groups=:
            description: An array of arrays of `WebhookFilter` objects used to determine which webhooks are triggered.
            nargs: -1
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
        --manual-creation: If manualCreation is true, CodeBuild doesn't create a webhook in GitHub and instead returns `payloadUrl` and `secret` values for the webhook.
        --no-manual-creation: If manualCreation is true, CodeBuild doesn't create a webhook in GitHub and instead returns `payloadUrl` and `secret` values for the webhook.
        --project-name=!: The name of the CodeBuild project.
        --pull-request-build-policy=: A PullRequestBuildPolicy object that defines comment-based approval requirements for triggering builds on pull requests.
        --scope-configuration=: The scope configuration for global or organization webhooks.
      completion:
        flag:
            build-type:
                - BUILD
                - BUILD_BATCH
                - RUNNER_BUILDKITE_BUILD
    - name: delete-build-batch
      description: Deletes a batch build.
      flags:
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
        --id=!: The identifier of the batch build to delete.
    - name: delete-fleet
      description: Deletes a compute fleet.
      flags:
        --arn=!: The ARN of the compute fleet.
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
    - name: delete-project
      description: Deletes a build project.
      flags:
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
        --name=!: The name of the build project.
    - name: delete-report
      description: Deletes a report.
      flags:
        --arn=!: The ARN of the report to delete.
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
    - name: delete-report-group
      description: Deletes a report group.
      flags:
        --arn=!: The ARN of the report group to delete.
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --delete-reports: If `true`, deletes any reports that belong to a report group before deleting the report group.
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
        --no-delete-reports: If `true`, deletes any reports that belong to a report group before deleting the report group.
    - name: delete-resource-policy
      description: Deletes a resource policy that is identified by its resource ARN.
      flags:
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
        --resource-arn=!: The ARN of the resource that is associated with the resource policy.
    - name: delete-source-credentials
      description: Deletes a set of GitHub, GitHub Enterprise, or Bitbucket source credentials.
      flags:
        --arn=!: The Amazon Resource Name (ARN) of the token.
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
    - name: delete-webhook
      description: For an existing CodeBuild build project that has its source code stored in a GitHub or Bitbucket repository, stops CodeBuild from rebuilding the source code every time a code change is pushed to the repository.
      flags:
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
        --project-name=!: The name of the CodeBuild project.
    - name: describe-code-coverages
      description: Retrieves one or more code coverage reports.
      flags:
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
        --max-items=: The  total number of items to return in the command's output.
        --max-line-coverage-percentage=: The maximum line coverage percentage to report.
        --max-results=: The maximum number of results to return.
        --min-line-coverage-percentage=: The minimum line coverage percentage to report.
        --next-token=: The `nextToken` value returned from a previous call to `DescribeCodeCoverages`.
        --page-size=: The size of each page to get in the AWS service call.
        --report-arn=!: The ARN of the report for which test cases are returned.
        --sort-by=: Specifies how the results are sorted.
        --sort-order=: Specifies if the results are sorted in ascending or descending order.
        --starting-token=: A token to specify where to start paginating.
      completion:
        flag:
            sort-by:
                - LINE_COVERAGE_PERCENTAGE
                - FILE_PATH
            sort-order:
                - ASCENDING
                - DESCENDING
    - name: describe-test-cases
      description: Returns a list of details about test cases for a report.
      flags:
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --filter=: A `TestCaseFilter` object used to filter the returned reports.
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
        --max-items=: The  total number of items to return in the command's output.
        --max-results=: The maximum number of paginated test cases returned per response.
        --next-token=: During a previous call, the maximum number of items that can be returned is the value specified in `maxResults`.
        --page-size=: The size of each page to get in the AWS service call.
        --report-arn=!: The ARN of the report for which test cases are returned.
        --starting-token=: A token to specify where to start paginating.
    - name: get-report-group-trend
      description: Analyzes and accumulates test report values for the specified test reports.
      flags:
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
        --num-of-reports=: The number of reports to analyze.
        --report-group-arn=!: The ARN of the report group that contains the reports to analyze.
        --trend-field=!: The test report value to accumulate.
      completion:
        flag:
            trend-field:
                - PASS_RATE
                - DURATION
                - TOTAL
                - LINE_COVERAGE
                - LINES_COVERED
                - LINES_MISSED
                - BRANCH_COVERAGE
                - BRANCHES_COVERED
                - BRANCHES_MISSED
    - name: get-resource-policy
      description: Gets a resource policy that is identified by its resource ARN.
      flags:
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
        --resource-arn=!: The ARN of the resource that is associated with the resource policy.
    - name: import-source-credentials
      description: Imports the source repository credentials for an CodeBuild project that has its source code stored in a GitHub, GitHub Enterprise, GitLab, GitLab Self Managed, or Bitbucket repository.
      flags:
        --auth-type=!: The type of authentication used to connect to a GitHub, GitHub Enterprise, GitLab, GitLab Self Managed, or Bitbucket repository.
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
        --no-should-overwrite: Set to `false` to prevent overwriting the repository source credentials.
        --server-type=!: The source provider used for this project.
        --should-overwrite: Set to `false` to prevent overwriting the repository source credentials.
        --token=!: For GitHub or GitHub Enterprise, this is the personal access token.
        --username=: The Bitbucket username when the `authType` is BASIC\_AUTH.
      completion:
        flag:
            auth-type:
                - OAUTH
                - BASIC_AUTH
                - PERSONAL_ACCESS_TOKEN
                - CODECONNECTIONS
                - SECRETS_MANAGER
            server-type:
                - GITHUB
                - BITBUCKET
                - GITHUB_ENTERPRISE
                - GITLAB
                - GITLAB_SELF_MANAGED
    - name: invalidate-project-cache
      description: Resets the cache for a project.
      flags:
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
        --project-name=!: The name of the CodeBuild build project that the cache is reset for.
    - name: list-build-batches
      description: Retrieves the identifiers of your build batches in the current region.
      flags:
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --filter=: A `BuildBatchFilter` object that specifies the filters for the search.
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
        --max-items=: The  total number of items to return in the command's output.
        --max-results=: The maximum number of results to return.
        --next-token=: The `nextToken` value returned from a previous call to `ListBuildBatches`.
        --page-size=: The size of each page to get in the AWS service call.
        --sort-order=: Specifies the sort order of the returned items.
        --starting-token=: A token to specify where to start paginating.
      completion:
        flag:
            sort-order:
                - ASCENDING
                - DESCENDING
    - name: list-build-batches-for-project
      description: Retrieves the identifiers of the build batches for a specific project.
      flags:
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --filter=: A `BuildBatchFilter` object that specifies the filters for the search.
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
        --max-items=: The  total number of items to return in the command's output.
        --max-results=: The maximum number of results to return.
        --next-token=: The `nextToken` value returned from a previous call to `ListBuildBatchesForProject`.
        --page-size=: The size of each page to get in the AWS service call.
        --project-name=: The name of the project.
        --sort-order=: Specifies the sort order of the returned items.
        --starting-token=: A token to specify where to start paginating.
      completion:
        flag:
            sort-order:
                - ASCENDING
                - DESCENDING
    - name: list-builds
      description: Gets a list of build IDs, with each build ID representing a single build.
      flags:
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
        --max-items=: The  total number of items to return in the command's output.
        --next-token=: During a previous call, if there are more than 100 items in the list, only the first 100 items are returned, along with a unique string called a *nextToken*.
        --sort-order=: The order to list build IDs.
        --starting-token=: A token to specify where to start paginating.
      completion:
        flag:
            sort-order:
                - ASCENDING
                - DESCENDING
    - name: list-builds-for-project
      description: Gets a list of build identifiers for the specified build project, with each build identifier representing a single build.
      flags:
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
        --max-items=: The  total number of items to return in the command's output.
        --next-token=: During a previous call, if there are more than 100 items in the list, only the first 100 items are returned, along with a unique string called a *nextToken*.
        --project-name=!: The name of the CodeBuild project.
        --sort-order=: The order to sort the results in.
        --starting-token=: A token to specify where to start paginating.
      completion:
        flag:
            sort-order:
                - ASCENDING
                - DESCENDING
    - name: list-command-executions-for-sandbox
      description: Gets a list of command executions for a sandbox.
      flags:
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
        --max-items=: The  total number of items to return in the command's output.
        --max-results=: The maximum number of sandbox records to be retrieved.
        --next-token=: The next token, if any, to get paginated results.
        --page-size=: The size of each page to get in the AWS service call.
        --sandbox-id=!: A `sandboxId` or `sandboxArn`.
        --sort-order=: The order in which sandbox records should be retrieved.
        --starting-token=: A token to specify where to start paginating.
      completion:
        flag:
            sort-order:
                - ASCENDING
                - DESCENDING
    - name: list-curated-environment-images
      description: Gets information about Docker images that are managed by CodeBuild.
      flags:
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
    - name: list-fleets
      description: Gets a list of compute fleet names with each compute fleet name representing a single compute fleet.
      flags:
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
        --max-results=: The maximum number of paginated compute fleets returned per response.
        --next-token=: During a previous call, if there are more than 100 items in the list, only the first 100 items are returned, along with a unique string called a *nextToken*.
        --sort-by=: The criterion to be used to list compute fleet names.
        --sort-order=: The order in which to list compute fleets.
      completion:
        flag:
            sort-by:
                - NAME
                - CREATED_TIME
                - LAST_MODIFIED_TIME
            sort-order:
                - ASCENDING
                - DESCENDING
    - name: list-projects
      description: Gets a list of build project names, with each build project name representing a single build project.
      flags:
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
        --max-items=: The  total number of items to return in the command's output.
        --next-token=: During a previous call, if there are more than 100 items in the list, only the first 100 items are returned, along with a unique string called a *nextToken*.
        --sort-by=: The criterion to be used to list build project names.
        --sort-order=: The order in which to list build projects.
        --starting-token=: A token to specify where to start paginating.
      completion:
        flag:
            sort-by:
                - NAME
                - CREATED_TIME
                - LAST_MODIFIED_TIME
            sort-order:
                - ASCENDING
                - DESCENDING
    - name: list-report-groups
      description: Gets a list ARNs for the report groups in the current Amazon Web Services account.
      flags:
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
        --max-items=: The  total number of items to return in the command's output.
        --max-results=: The maximum number of paginated report groups returned per response.
        --next-token=: During a previous call, the maximum number of items that can be returned is the value specified in `maxResults`.
        --page-size=: The size of each page to get in the AWS service call.
        --sort-by=: The criterion to be used to list build report groups.
        --sort-order=: Used to specify the order to sort the list of returned report groups.
        --starting-token=: A token to specify where to start paginating.
      completion:
        flag:
            sort-by:
                - NAME
                - CREATED_TIME
                - LAST_MODIFIED_TIME
            sort-order:
                - ASCENDING
                - DESCENDING
    - name: list-reports
      description: Returns a list of ARNs for the reports in the current Amazon Web Services account.
      flags:
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --filter=: A `ReportFilter` object used to filter the returned reports.
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
        --max-items=: The  total number of items to return in the command's output.
        --max-results=: The maximum number of paginated reports returned per response.
        --next-token=: During a previous call, the maximum number of items that can be returned is the value specified in `maxResults`.
        --page-size=: The size of each page to get in the AWS service call.
        --sort-order=: Specifies the sort order for the list of returned reports.
        --starting-token=: A token to specify where to start paginating.
      completion:
        flag:
            sort-order:
                - ASCENDING
                - DESCENDING
    - name: list-reports-for-report-group
      description: Returns a list of ARNs for the reports that belong to a `ReportGroup`.
      flags:
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --filter=: A `ReportFilter` object used to filter the returned reports.
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
        --max-items=: The  total number of items to return in the command's output.
        --max-results=: The maximum number of paginated reports in this report group returned per response.
        --next-token=: During a previous call, the maximum number of items that can be returned is the value specified in `maxResults`.
        --page-size=: The size of each page to get in the AWS service call.
        --report-group-arn=!: The ARN of the report group for which you want to return report ARNs.
        --sort-order=: Use to specify whether the results are returned in ascending or descending order.
        --starting-token=: A token to specify where to start paginating.
      completion:
        flag:
            sort-order:
                - ASCENDING
                - DESCENDING
    - name: list-sandboxes
      description: Gets a list of sandboxes.
      flags:
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
        --max-items=: The  total number of items to return in the command's output.
        --max-results=: The maximum number of sandbox records to be retrieved.
        --next-token=: The next token, if any, to get paginated results.
        --page-size=: The size of each page to get in the AWS service call.
        --sort-order=: The order in which sandbox records should be retrieved.
        --starting-token=: A token to specify where to start paginating.
      completion:
        flag:
            sort-order:
                - ASCENDING
                - DESCENDING
    - name: list-sandboxes-for-project
      description: Gets a list of sandboxes for a given project.
      flags:
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
        --max-items=: The  total number of items to return in the command's output.
        --max-results=: The maximum number of sandbox records to be retrieved.
        --next-token=: The next token, if any, to get paginated results.
        --page-size=: The size of each page to get in the AWS service call.
        --project-name=!: The CodeBuild project name.
        --sort-order=: The order in which sandbox records should be retrieved.
        --starting-token=: A token to specify where to start paginating.
      completion:
        flag:
            sort-order:
                - ASCENDING
                - DESCENDING
    - name: list-shared-projects
      description: Gets a list of projects that are shared with other Amazon Web Services accounts or users.
      flags:
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
        --max-items=: The  total number of items to return in the command's output.
        --max-results=: The maximum number of paginated shared build projects returned per response.
        --next-token=: During a previous call, the maximum number of items that can be returned is the value specified in `maxResults`.
        --page-size=: The size of each page to get in the AWS service call.
        --sort-by=: The criterion to be used to list build projects shared with the current Amazon Web Services account or user.
        --sort-order=: The order in which to list shared build projects.
        --starting-token=: A token to specify where to start paginating.
      completion:
        flag:
            sort-by:
                - ARN
                - MODIFIED_TIME
            sort-order:
                - ASCENDING
                - DESCENDING
    - name: list-shared-report-groups
      description: Gets a list of report groups that are shared with other Amazon Web Services accounts or users.
      flags:
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
        --max-items=: The  total number of items to return in the command's output.
        --max-results=: The maximum number of paginated shared report groups per response.
        --next-token=: During a previous call, the maximum number of items that can be returned is the value specified in `maxResults`.
        --page-size=: The size of each page to get in the AWS service call.
        --sort-by=: The criterion to be used to list report groups shared with the current Amazon Web Services account or user.
        --sort-order=: The order in which to list shared report groups.
        --starting-token=: A token to specify where to start paginating.
      completion:
        flag:
            sort-by:
                - ARN
                - MODIFIED_TIME
            sort-order:
                - ASCENDING
                - DESCENDING
    - name: list-source-credentials
      description: Returns a list of `SourceCredentialsInfo` objects.
      flags:
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
    - name: put-resource-policy
      description: Stores a resource policy for the ARN of a `Project` or `ReportGroup` object.
      flags:
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
        --policy=!: A JSON-formatted resource policy.
        --resource-arn=!: The ARN of the `Project` or `ReportGroup` resource you want to associate with a resource policy.
    - name: retry-build
      description: Restarts a build.
      flags:
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
        --id=: Specifies the identifier of the build to restart.
        --idempotency-token=: A unique, case sensitive identifier you provide to ensure the idempotency of the `RetryBuild` request.
    - name: retry-build-batch
      description: Restarts a failed batch build.
      flags:
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
        --id=: Specifies the identifier of the batch build to restart.
        --idempotency-token=: A unique, case sensitive identifier you provide to ensure the idempotency of the `RetryBuildBatch` request.
        --retry-type=: Specifies the type of retry to perform.
      completion:
        flag:
            retry-type:
                - RETRY_ALL_BUILDS
                - RETRY_FAILED_BUILDS
    - name: start-build
      description: Starts running a build with the settings defined in the project.
      flags:
        --artifacts-override=: Build output artifact settings that override, for this build only, the latest ones already defined in the build project.
        --auto-retry-limit-override=: The maximum number of additional automatic retries after a failed build.
        --build-status-config-override=: Contains information that defines how the build project reports the build status to the source provider.
        --buildspec-override=: A buildspec file declaration that overrides the latest one defined in the build project, for this build only.
        --cache-override=: A ProjectCache object specified for this build that overrides the one defined in the build project.
        --certificate-override=: The name of a certificate for this build that overrides the one specified in the build project.
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --compute-type-override=: The name of a compute type for this build that overrides the one specified in the build project.
        --debug-session-enabled: Specifies if session debugging is enabled for this build.
        --encryption-key-override=: The Key Management Service customer master key (CMK) that overrides the one specified in the build project.
        --environment-type-override=: A container type for this build that overrides the one specified in the build project.
        --environment-variables-override=:
            description: A set of environment variables that overrides, for this build only, the latest ones already defined in the build project.
            nargs: -1
        --fleet-override=: A ProjectFleet object specified for this build that overrides the one defined in the build project.
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
        --git-clone-depth-override=: The user-defined depth of history, with a minimum value of 0, that overrides, for this build only, any previous depth of history defined in the build project.
        --git-submodules-config-override=: Information about the Git submodules configuration for this build of an CodeBuild build project.
        --idempotency-token=: A unique, case sensitive identifier you provide to ensure the idempotency of the StartBuild request.
        --image-override=: The name of an image for this build that overrides the one specified in the build project.
        --image-pull-credentials-type-override=: The type of credentials CodeBuild uses to pull images in your build.
        --insecure-ssl-override: Enable this flag to override the insecure SSL setting that is specified in the build project.
        --logs-config-override=: Log settings for this build that override the log settings defined in the build project.
        --no-debug-session-enabled: Specifies if session debugging is enabled for this build.
        --no-insecure-ssl-override: Enable this flag to override the insecure SSL setting that is specified in the build project.
        --no-privileged-mode-override: Enable this flag to override privileged mode in the build project.
        --no-report-build-status-override: Set to true to report to your source provider the status of a build's start and completion.
        --privileged-mode-override: Enable this flag to override privileged mode in the build project.
        --project-name=!: The name of the CodeBuild build project to start running a build.
        --queued-timeout-in-minutes-override=: The number of minutes a build is allowed to be queued before it times out.
        --registry-credential-override=: The credentials for access to a private registry.
        --report-build-status-override: Set to true to report to your source provider the status of a build's start and completion.
        --secondary-artifacts-override=:
            description: An array of `ProjectArtifacts` objects.
            nargs: -1
        --secondary-sources-override=:
            description: An array of `ProjectSource` objects.
            nargs: -1
        --secondary-sources-version-override=:
            description: An array of `ProjectSourceVersion` objects that specify one or more versions of the project's secondary sources to be used for this build only.
            nargs: -1
        --service-role-override=: The name of a service role for this build that overrides the one specified in the build project.
        --source-auth-override=: An authorization type for this build that overrides the one defined in the build project.
        --source-location-override=: A location that overrides, for this build, the source location for the one defined in the build project.
        --source-type-override=: A source input type, for this build, that overrides the source input defined in the build project.
        --source-version=: The version of the build input to be built, for this build only.
        --timeout-in-minutes-override=: The number of build timeout minutes, from 5 to 2160 (36 hours), that overrides, for this build only, the latest setting already defined in the build project.
      completion:
        flag:
            compute-type-override:
                - BUILD_GENERAL1_SMALL
                - BUILD_GENERAL1_MEDIUM
                - BUILD_GENERAL1_LARGE
                - BUILD_GENERAL1_XLARGE
                - BUILD_GENERAL1_2XLARGE
                - BUILD_LAMBDA_1GB
                - BUILD_LAMBDA_2GB
                - BUILD_LAMBDA_4GB
                - BUILD_LAMBDA_8GB
                - BUILD_LAMBDA_10GB
                - ATTRIBUTE_BASED_COMPUTE
                - CUSTOM_INSTANCE_TYPE
            environment-type-override:
                - WINDOWS_CONTAINER
                - LINUX_CONTAINER
                - LINUX_GPU_CONTAINER
                - ARM_CONTAINER
                - WINDOWS_SERVER_2019_CONTAINER
                - WINDOWS_SERVER_2022_CONTAINER
                - LINUX_LAMBDA_CONTAINER
                - ARM_LAMBDA_CONTAINER
                - LINUX_EC2
                - ARM_EC2
                - WINDOWS_EC2
                - MAC_ARM
            image-pull-credentials-type-override:
                - CODEBUILD
                - SERVICE_ROLE
            source-type-override:
                - CODECOMMIT
                - CODEPIPELINE
                - GITHUB
                - GITLAB
                - GITLAB_SELF_MANAGED
                - S3
                - BITBUCKET
                - GITHUB_ENTERPRISE
                - NO_SOURCE
    - name: start-build-batch
      description: Starts a batch build for a project.
      flags:
        --artifacts-override=: An array of `ProjectArtifacts` objects that contains information about the build output artifact overrides for the build project.
        --build-batch-config-override=: A `BuildBatchConfigOverride` object that contains batch build configuration overrides.
        --build-timeout-in-minutes-override=: Overrides the build timeout specified in the batch build project.
        --buildspec-override=: A buildspec file declaration that overrides, for this build only, the latest one already defined in the build project.
        --cache-override=: A `ProjectCache` object that specifies cache overrides.
        --certificate-override=: The name of a certificate for this batch build that overrides the one specified in the batch build project.
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --compute-type-override=: The name of a compute type for this batch build that overrides the one specified in the batch build project.
        --debug-session-enabled: Specifies if session debugging is enabled for this batch build.
        --encryption-key-override=: The Key Management Service customer master key (CMK) that overrides the one specified in the batch build project.
        --environment-type-override=: A container type for this batch build that overrides the one specified in the batch build project.
        --environment-variables-override=:
            description: An array of `EnvironmentVariable` objects that override, or add to, the environment variables defined in the batch build project.
            nargs: -1
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
        --git-clone-depth-override=: The user-defined depth of history, with a minimum value of 0, that overrides, for this batch build only, any previous depth of history defined in the batch build project.
        --git-submodules-config-override=: A `GitSubmodulesConfig` object that overrides the Git submodules configuration for this batch build.
        --idempotency-token=: A unique, case sensitive identifier you provide to ensure the idempotency of the `StartBuildBatch` request.
        --image-override=: The name of an image for this batch build that overrides the one specified in the batch build project.
        --image-pull-credentials-type-override=: The type of credentials CodeBuild uses to pull images in your batch build.
        --insecure-ssl-override: Enable this flag to override the insecure SSL setting that is specified in the batch build project.
        --logs-config-override=: A `LogsConfig` object that override the log settings defined in the batch build project.
        --no-debug-session-enabled: Specifies if session debugging is enabled for this batch build.
        --no-insecure-ssl-override: Enable this flag to override the insecure SSL setting that is specified in the batch build project.
        --no-privileged-mode-override: Enable this flag to override privileged mode in the batch build project.
        --no-report-build-batch-status-override: Set to `true` to report to your source provider the status of a batch build's start and completion.
        --privileged-mode-override: Enable this flag to override privileged mode in the batch build project.
        --project-name=!: The name of the project.
        --queued-timeout-in-minutes-override=: The number of minutes a batch build is allowed to be queued before it times out.
        --registry-credential-override=: A `RegistryCredential` object that overrides credentials for access to a private registry.
        --report-build-batch-status-override: Set to `true` to report to your source provider the status of a batch build's start and completion.
        --secondary-artifacts-override=:
            description: An array of `ProjectArtifacts` objects that override the secondary artifacts defined in the batch build project.
            nargs: -1
        --secondary-sources-override=:
            description: An array of `ProjectSource` objects that override the secondary sources defined in the batch build project.
            nargs: -1
        --secondary-sources-version-override=:
            description: An array of `ProjectSourceVersion` objects that override the secondary source versions in the batch build project.
            nargs: -1
        --service-role-override=: The name of a service role for this batch build that overrides the one specified in the batch build project.
        --source-auth-override=: A `SourceAuth` object that overrides the one defined in the batch build project.
        --source-location-override=: A location that overrides, for this batch build, the source location defined in the batch build project.
        --source-type-override=: The source input type that overrides the source input defined in the batch build project.
        --source-version=: The version of the batch build input to be built, for this build only.
      completion:
        flag:
            compute-type-override:
                - BUILD_GENERAL1_SMALL
                - BUILD_GENERAL1_MEDIUM
                - BUILD_GENERAL1_LARGE
                - BUILD_GENERAL1_XLARGE
                - BUILD_GENERAL1_2XLARGE
                - BUILD_LAMBDA_1GB
                - BUILD_LAMBDA_2GB
                - BUILD_LAMBDA_4GB
                - BUILD_LAMBDA_8GB
                - BUILD_LAMBDA_10GB
                - ATTRIBUTE_BASED_COMPUTE
                - CUSTOM_INSTANCE_TYPE
            environment-type-override:
                - WINDOWS_CONTAINER
                - LINUX_CONTAINER
                - LINUX_GPU_CONTAINER
                - ARM_CONTAINER
                - WINDOWS_SERVER_2019_CONTAINER
                - WINDOWS_SERVER_2022_CONTAINER
                - LINUX_LAMBDA_CONTAINER
                - ARM_LAMBDA_CONTAINER
                - LINUX_EC2
                - ARM_EC2
                - WINDOWS_EC2
                - MAC_ARM
            image-pull-credentials-type-override:
                - CODEBUILD
                - SERVICE_ROLE
            source-type-override:
                - CODECOMMIT
                - CODEPIPELINE
                - GITHUB
                - GITLAB
                - GITLAB_SELF_MANAGED
                - S3
                - BITBUCKET
                - GITHUB_ENTERPRISE
                - NO_SOURCE
    - name: start-command-execution
      description: Starts a command execution.
      flags:
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --command=!: The command that needs to be executed.
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
        --sandbox-id=!: A `sandboxId` or `sandboxArn`.
        --type=: The command type.
      completion:
        flag:
            type:
                - SHELL
    - name: start-sandbox
      description: Starts a sandbox.
      flags:
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
        --idempotency-token=: A unique client token.
        --project-name=: The CodeBuild project name.
    - name: start-sandbox-connection
      description: Starts a sandbox connection.
      flags:
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
        --sandbox-id=!: A `sandboxId` or `sandboxArn`.
    - name: stop-build
      description: Attempts to stop running a build.
      flags:
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
        --id=!: The ID of the build.
    - name: stop-build-batch
      description: Stops a running batch build.
      flags:
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
        --id=!: The identifier of the batch build to stop.
    - name: stop-sandbox
      description: Stops a sandbox.
      flags:
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
        --id=!: Information about the requested sandbox ID.
    - name: update-fleet
      description: Updates a compute fleet.
      flags:
        --arn=!: The ARN of the compute fleet.
        --base-capacity=: The initial number of machines allocated to the compute ﬂeet, which deﬁnes the number of builds that can run in parallel.
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --compute-configuration=: The compute configuration of the compute fleet.
        --compute-type=: Information about the compute resources the compute fleet uses.
        --environment-type=: The environment type of the compute fleet.
        --fleet-service-role=: The service role associated with the compute fleet.
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
        --image-id=: The Amazon Machine Image (AMI) of the compute fleet.
        --overflow-behavior=: The compute fleet overflow behavior.
        --proxy-configuration=: The proxy configuration of the compute fleet.
        --scaling-configuration=: The scaling configuration of the compute fleet.
        --tags=:
            description: A list of tag key and value pairs associated with this compute fleet.
            nargs: -1
        --vpc-config=: ""
      completion:
        flag:
            compute-type:
                - BUILD_GENERAL1_SMALL
                - BUILD_GENERAL1_MEDIUM
                - BUILD_GENERAL1_LARGE
                - BUILD_GENERAL1_XLARGE
                - BUILD_GENERAL1_2XLARGE
                - BUILD_LAMBDA_1GB
                - BUILD_LAMBDA_2GB
                - BUILD_LAMBDA_4GB
                - BUILD_LAMBDA_8GB
                - BUILD_LAMBDA_10GB
                - ATTRIBUTE_BASED_COMPUTE
                - CUSTOM_INSTANCE_TYPE
            environment-type:
                - WINDOWS_CONTAINER
                - LINUX_CONTAINER
                - LINUX_GPU_CONTAINER
                - ARM_CONTAINER
                - WINDOWS_SERVER_2019_CONTAINER
                - WINDOWS_SERVER_2022_CONTAINER
                - LINUX_LAMBDA_CONTAINER
                - ARM_LAMBDA_CONTAINER
                - LINUX_EC2
                - ARM_EC2
                - WINDOWS_EC2
                - MAC_ARM
            overflow-behavior:
                - QUEUE
                - ON_DEMAND
    - name: update-project
      description: Changes the settings of a build project.
      flags:
        --artifacts=: Information to be changed about the build output artifacts for the build project.
        --auto-retry-limit=: The maximum number of additional automatic retries after a failed build.
        --badge-enabled: Set this to true to generate a publicly accessible URL for your project's build badge.
        --build-batch-config=: ""
        --cache=: Stores recently used information so that it can be quickly accessed at a later time.
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --concurrent-build-limit=: The maximum number of concurrent builds that are allowed for this project.
        --description=: A new or replacement description of the build project.
        --encryption-key=: The Key Management Service customer master key (CMK) to be used for encrypting the build output artifacts.
        --environment=: Information to be changed about the build environment for the build project.
        --file-system-locations=:
            description: An array of `ProjectFileSystemLocation` objects for a CodeBuild build project.
            nargs: -1
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
        --logs-config=: Information about logs for the build project.
        --name=!: The name of the build project.
        --no-badge-enabled: Set this to true to generate a publicly accessible URL for your project's build badge.
        --queued-timeout-in-minutes=: The number of minutes a build is allowed to be queued before it times out.
        --secondary-artifacts=:
            description: An array of `ProjectArtifact` objects.
            nargs: -1
        --secondary-source-versions=:
            description: An array of `ProjectSourceVersion` objects.
            nargs: -1
        --secondary-sources=:
            description: An array of `ProjectSource` objects.
            nargs: -1
        --service-role=: The replacement ARN of the IAM role that enables CodeBuild to interact with dependent Amazon Web Services services on behalf of the Amazon Web Services account.
        --source-version=: A version of the build input to be built for this project.
        --source=: Information to be changed about the build input source code for the build project.
        --tags=:
            description: An updated list of tag key and value pairs associated with this build project.
            nargs: -1
        --timeout-in-minutes=: The replacement value in minutes, from 5 to 2160 (36 hours), for CodeBuild to wait before timing out any related build that did not get marked as completed.
        --vpc-config=: VpcConfig enables CodeBuild to access resources in an Amazon VPC.
    - name: update-project-visibility
      description: Changes the public visibility for a project.
      flags:
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
        --project-arn=!: The Amazon Resource Name (ARN) of the build project.
        --project-visibility=!: ""
        --resource-access-role=: The ARN of the IAM role that enables CodeBuild to access the CloudWatch Logs and Amazon S3 artifacts for the project's builds.
      completion:
        flag:
            project-visibility:
                - PUBLIC_READ
                - PRIVATE
    - name: update-report-group
      description: Updates a report group.
      flags:
        --arn=!: The ARN of the report group to update.
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --export-config=: Used to specify an updated export type.
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
        --tags=:
            description: An updated list of tag key and value pairs associated with this report group.
            nargs: -1
    - name: update-webhook
      description: Updates the webhook associated with an CodeBuild build project.
      flags:
        --branch-filter=: A regular expression used to determine which repository branches are built when a webhook is triggered.
        --build-type=: Specifies the type of build this webhook will trigger.
        --cli-input-json=: Read arguments from the JSON string provided.
        --cli-input-yaml=: Read arguments from the YAML string provided.
        --filter-groups=:
            description: An array of arrays of `WebhookFilter` objects used to determine if a webhook event can trigger a build.
            nargs: -1
        --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
        --no-rotate-secret: A boolean value that specifies whether the associated GitHub repository's secret token should be updated.
        --project-name=!: The name of the CodeBuild project.
        --pull-request-build-policy=: A PullRequestBuildPolicy object that defines comment-based approval requirements for triggering builds on pull requests.
        --rotate-secret: A boolean value that specifies whether the associated GitHub repository's secret token should be updated.
      completion:
        flag:
            build-type:
                - BUILD
                - BUILD_BATCH
                - RUNNER_BUILDKITE_BUILD
