name: create-sbom-export
description: Creates a software bill of materials (SBOM) report.
flags:
    --cli-input-json=: Read arguments from the JSON string provided.
    --cli-input-yaml=: Read arguments from the YAML string provided.
    --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
    --report-format=!: The output format for the software bill of materials (SBOM) report.
    --resource-filter-criteria=: The resource filter criteria for the software bill of materials (SBOM) report.
    --s3-destination=!: Contains details of the Amazon S3 bucket and KMS key used to export findings.
completion:
    flag:
        report-format:
            - CYCLONEDX_1_4
            - SPDX_2_3
