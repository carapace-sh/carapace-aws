name: get-user-profile
description: Gets a user profile in Amazon DataZone.
flags:
    --cli-input-json=: Read arguments from the JSON string provided
    --cli-input-yaml=: Read arguments from the YAML string provided
    --domain-identifier=!: the ID of the Amazon DataZone domain the data portal of which you want to get.
    --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request
    --type=: The type of the user profile.
    --user-identifier=!: The identifier of the user for which you want to get the user profile.
completion:
    flag:
        type:
            - IAM
            - SSO
