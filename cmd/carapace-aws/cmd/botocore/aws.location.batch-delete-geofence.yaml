name: batch-delete-geofence
description: Deletes a batch of geofences from a geofence collection.
flags:
    --collection-name=!: The geofence collection storing the geofences to be deleted.
    --geofence-ids=!: The batch of geofences to be deleted.
