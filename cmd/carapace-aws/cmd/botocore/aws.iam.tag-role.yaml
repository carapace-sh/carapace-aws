name: tag-role
description: Adds one or more tags to an IAM role.
flags:
    --role-name=!: The name of the IAM role to which you want to add tags.
    --tags=!: The list of tags that you want to attach to the IAM role.
