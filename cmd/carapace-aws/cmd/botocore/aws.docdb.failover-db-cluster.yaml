name: failover-db-cluster
description: Forces a failover for a cluster.
flags:
    --db-cluster-identifier=: A cluster identifier to force a failover for.
    --target-db-instance-identifier=: The name of the instance to promote to the primary instance.
