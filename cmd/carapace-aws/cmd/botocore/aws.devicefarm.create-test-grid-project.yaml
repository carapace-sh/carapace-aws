name: create-test-grid-project
description: Creates a Selenium testing project.
flags:
    --cli-input-json=: Read arguments from the JSON string provided.
    --cli-input-yaml=: Read arguments from the YAML string provided.
    --description=: Human-readable description of the project.
    --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
    --name=!: Human-readable name of the Selenium testing project.
    --vpc-config=: The VPC security groups and subnets that are attached to a project.
