name: list-collaboration-privacy-budgets
description: Returns an array that summarizes each privacy budget in a specified collaboration.
flags:
    --access-budget-resource-arn=: The Amazon Resource Name (ARN) of the Configured Table Association (ConfiguredTableAssociation) used to filter privacy budgets.
    --collaboration-identifier=!: A unique identifier for one of your collaborations.
    --max-results=: The maximum number of results that are returned for an API request call.
    --next-token=: The pagination token that's used to fetch the next set of results.
    --privacy-budget-type=!: Specifies the type of the privacy budget.
completion:
    flag:
        privacy-budget-type:
            - DIFFERENTIAL_PRIVACY
            - ACCESS_BUDGET
