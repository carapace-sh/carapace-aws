name: update-anomaly-monitor
description: Updates an existing cost anomaly monitor.
flags:
    --monitor-arn=!: Cost anomaly monitor Amazon Resource Names (ARNs).
    --monitor-name=: The new name for the cost anomaly monitor.
