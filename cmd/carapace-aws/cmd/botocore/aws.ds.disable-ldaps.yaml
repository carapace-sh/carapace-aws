name: disable-ldaps
description: Deactivates LDAP secure calls for the specified directory.
flags:
    --directory-id=!: The identifier of the directory.
    --type=!: The type of LDAP security to enable.
completion:
    flag:
        type:
            - Client
