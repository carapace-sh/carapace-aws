name: batch-get-jobs
description: Returns a list of resource metadata for a given list of job names.
flags:
    --job-names=!: A list of job names, which might be the names returned from the `ListJobs` operation.
