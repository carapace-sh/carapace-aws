name: delete-account-audit-configuration
description: Restores the default settings for Device Defender audits for this account.
flags:
    --delete-scheduled-audits=: If true, all scheduled audits are deleted.
