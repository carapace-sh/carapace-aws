name: get-ipam-policy-allocation-rules
description: Gets the allocation rules for an IPAM policy.
flags:
    --cli-input-json=: Read arguments from the JSON string provided.
    --cli-input-yaml=: Read arguments from the YAML string provided.
    --dry-run: A check for whether you have the required permissions for the action without actually making the request and provides an error response.
    --filters=: One or more filters for the allocation rules.
    --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
    --ipam-policy-id=!: The ID of the IPAM policy for which to get allocation rules.
    --locale=: The locale for which to get the allocation rules.
    --max-results=: The maximum number of results to return in a single call.
    --next-token=: The token for the next page of results.
    --no-dry-run: A check for whether you have the required permissions for the action without actually making the request and provides an error response.
    --resource-type=: The resource type for which to get the allocation rules.
completion:
    flag:
        resource-type:
            - alb
            - eip
            - rds
            - rnat
