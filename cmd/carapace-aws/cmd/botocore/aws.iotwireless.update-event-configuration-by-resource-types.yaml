name: update-event-configuration-by-resource-types
description: Update the event configuration based on resource types.
flags:
    --cli-input-json=: Read arguments from the JSON string provided.
    --cli-input-yaml=: Read arguments from the YAML string provided.
    --connection-status=: Connection status resource type event configuration object for enabling and disabling wireless gateway topic.
    --device-registration-state=: Device registration state resource type event configuration object for enabling and disabling wireless gateway topic.
    --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
    --join=: Join resource type event configuration object for enabling and disabling wireless device topic.
    --message-delivery-status=: Message delivery status resource type event configuration object for enabling and disabling wireless device topic.
    --proximity=: Proximity resource type event configuration object for enabling and disabling wireless gateway topic.
