name: describe-mlmodels
description: Returns a list of `MLModel` that match the search criteria in the request.
flags:
    --eq=: The equal to operator.
    --filter-variable=: 'Use one of the following variables to filter a list of `MLModel`:'
    --ge=: The greater than or equal to operator.
    --gt=: The greater than operator.
    --le=: The less than or equal to operator.
    --limit=: The number of pages of information to include in the result.
    --lt=: The less than operator.
    --ne=: The not equal to operator.
    --next-token=: The ID of the page in the paginated results.
    --prefix=: A string that is found at the beginning of a variable, such as `Name` or `Id`.
    --sort-order=: A two-value parameter that determines the sequence of the resulting list of `MLModel`.
completion:
    flag:
        filter-variable:
            - CreatedAt
            - LastUpdatedAt
            - Status
            - Name
            - IAMUser
            - TrainingDataSourceId
            - RealtimeEndpointStatus
            - MLModelType
            - Algorithm
            - TrainingDataURI
        sort-order:
            - asc
            - dsc
