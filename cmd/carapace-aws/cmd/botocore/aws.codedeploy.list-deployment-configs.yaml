name: list-deployment-configs
description: Lists the deployment configurations with the user or Amazon Web Services account.
flags:
    --next-token=: An identifier returned from the previous `ListDeploymentConfigs` call.
