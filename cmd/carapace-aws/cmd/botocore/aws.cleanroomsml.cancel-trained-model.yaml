name: cancel-trained-model
description: Submits a request to cancel the trained model job.
flags:
    --membership-identifier=!: The membership ID of the trained model job that you want to cancel.
    --trained-model-arn=!: The Amazon Resource Name (ARN) of the trained model job that you want to cancel.
    --version-identifier=: The version identifier of the trained model to cancel.
