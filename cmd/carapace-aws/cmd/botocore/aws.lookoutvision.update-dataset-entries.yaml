name: update-dataset-entries
description: Adds or updates one or more JSON Line entries in a dataset.
flags:
    --changes=!: The entries to add to the dataset.
    --cli-input-json=: Read arguments from the JSON string provided.
    --cli-input-yaml=: Read arguments from the YAML string provided.
    --client-token=: ClientToken is an idempotency token that ensures a call to `UpdateDatasetEntries` completes only once.
    --dataset-type=!: The type of the dataset that you want to update.
    --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
    --project-name=!: The name of the project that contains the dataset that you want to update.
