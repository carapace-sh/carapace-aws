name: execute-query
description: Run SQL queries to retrieve metadata and time-series data from asset models, assets, measurements, metrics, transforms, and aggregates.
flags:
    --client-token=: A unique case-sensitive identifier that you can provide to ensure the idempotency of the request.
    --max-results=: The maximum number of results to return at one time.
    --next-token=: The string that specifies the next page of results.
    --query-statement=!: The IoT SiteWise query statement.
