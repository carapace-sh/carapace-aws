name: update-export
description: Updates the password used to protect an export zip archive.
flags:
    --export-id=!: The unique identifier Amazon Lex assigned to the export.
    --file-password=: The new password to use to encrypt the export zip archive.
