name: delete-availability-configuration
description: Deletes the `AvailabilityConfiguration` for the given WorkMail organization and domain.
flags:
    --cli-input-json=: Read arguments from the JSON string provided
    --cli-input-yaml=: Read arguments from the YAML string provided
    --domain-name=!: The domain for which the `AvailabilityConfiguration` will be deleted.
    --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request
    --organization-id=!: The WorkMail organization for which the `AvailabilityConfiguration` will be deleted.
