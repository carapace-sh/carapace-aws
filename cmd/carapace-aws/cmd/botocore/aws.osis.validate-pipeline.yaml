name: validate-pipeline
description: Checks whether an OpenSearch Ingestion pipeline configuration is valid prior to creation.
flags:
    --pipeline-configuration-body=!: The pipeline configuration in YAML format.
