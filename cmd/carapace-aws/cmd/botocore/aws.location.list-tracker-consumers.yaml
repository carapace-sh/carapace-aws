name: list-tracker-consumers
description: Lists geofence collections currently associated to the given tracker resource.
flags:
    --max-results=: An optional limit for the number of resources returned in a single call.
    --next-token=: The pagination token specifying which page of results to return in the response.
    --tracker-name=!: The tracker resource whose associated geofence collections you want to list.
