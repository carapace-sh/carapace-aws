name: list-call-analytics-jobs
description: Provides a list of Call Analytics jobs that match the specified criteria.
flags:
    --cli-input-json=: Read arguments from the JSON string provided
    --cli-input-yaml=: Read arguments from the YAML string provided
    --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request
    --job-name-contains=: Returns only the Call Analytics jobs that contain the specified string.
    --max-results=: The maximum number of Call Analytics jobs to return in each page of results.
    --next-token=: If your `ListCallAnalyticsJobs` request returns more results than can be displayed, `NextToken` is displayed in the response with an associated string.
    --status=: Returns only Call Analytics jobs with the specified status.
completion:
    flag:
        status:
            - QUEUED
            - IN_PROGRESS
            - FAILED
            - COMPLETED
