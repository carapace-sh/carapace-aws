name: update-service-network
description: Updates the specified service network.
flags:
    --auth-type=!: The type of IAM policy.
    --service-network-identifier=!: The ID or ARN of the service network.
completion:
    flag:
        auth-type:
            - NONE
            - AWS_IAM
