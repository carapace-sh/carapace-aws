name: delete-project
description: Deletes a project in Amazon DataZone.
flags:
    --cli-input-json=: Read arguments from the JSON string provided.
    --cli-input-yaml=: Read arguments from the YAML string provided.
    --domain-identifier=!: The ID of the Amazon DataZone domain in which the project is deleted.
    --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
    --identifier=!: The identifier of the project that is to be deleted.
    --no-skip-deletion-check: Specifies the optional flag to delete all child entities within the project.
    --skip-deletion-check: Specifies the optional flag to delete all child entities within the project.
