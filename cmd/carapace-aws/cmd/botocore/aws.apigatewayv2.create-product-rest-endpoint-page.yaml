name: create-product-rest-endpoint-page
description: Creates a product REST endpoint page for a portal product.
flags:
    --cli-input-json=: Read arguments from the JSON string provided.
    --cli-input-yaml=: Read arguments from the YAML string provided.
    --display-content=: The content of the product REST endpoint page.
    --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
    --portal-product-id=!: The portal product identifier.
    --rest-endpoint-identifier=!: The REST endpoint identifier.
    --try-it-state=: The try it state of the product REST endpoint page.
completion:
    flag:
        try-it-state:
            - ENABLED
            - DISABLED
