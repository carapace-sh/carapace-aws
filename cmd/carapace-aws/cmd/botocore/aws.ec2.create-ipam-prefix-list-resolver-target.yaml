name: create-ipam-prefix-list-resolver-target
description: Creates an IPAM prefix list resolver target.
flags:
    --client-token=: A unique, case-sensitive identifier that you provide to ensure the idempotency of the request.
    --desired-version=: The specific version of the prefix list to target.
    --dry-run: A check for whether you have the required permissions for the action without actually making the request and provides an error response.
    --ipam-prefix-list-resolver-id=!: The ID of the IPAM prefix list resolver that will manage the synchronization of CIDRs to the target prefix list.
    --no-dry-run&: A check for whether you have the required permissions for the action without actually making the request and provides an error response.
    --no-track-latest-version!&: Indicates whether the resolver target should automatically track the latest version of the prefix list.
    --prefix-list-id=!: The ID of the managed prefix list that will be synchronized with CIDRs selected by the IPAM prefix list resolver.
    --prefix-list-region=!: The Amazon Web Services Region where the prefix list is located.
    --tag-specifications=: The tags to apply to the IPAM prefix list resolver target during creation.
    --track-latest-version!: Indicates whether the resolver target should automatically track the latest version of the prefix list.
