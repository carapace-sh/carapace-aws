name: update-fleet-metric
description: Updates the data for a fleet metric.
flags:
    --aggregation-field=: The field to aggregate.
    --aggregation-type=: The type of the aggregation query.
    --cli-input-json=: Read arguments from the JSON string provided
    --cli-input-yaml=: Read arguments from the YAML string provided
    --description=: The description of the fleet metric.
    --expected-version=: The expected version of the fleet metric record in the registry.
    --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request
    --index-name=!: The name of the index to search.
    --metric-name=!: The name of the fleet metric to update.
    --period=: The time in seconds between fleet metric emissions.
    --query-string=: The search query string.
    --query-version=: The version of the query.
    --unit=: Used to support unit transformation such as milliseconds to seconds.
completion:
    flag:
        unit:
            - Seconds
            - Microseconds
            - Milliseconds
            - Bytes
            - Kilobytes
            - Megabytes
            - Gigabytes
            - Terabytes
            - Bits
            - Kilobits
            - Megabits
            - Gigabits
            - Terabits
            - Percent
            - Count
            - Bytes/Second
            - Kilobytes/Second
            - Megabytes/Second
            - Gigabytes/Second
            - Terabytes/Second
            - Bits/Second
            - Kilobits/Second
            - Megabits/Second
            - Gigabits/Second
            - Terabits/Second
            - Count/Second
            - None
