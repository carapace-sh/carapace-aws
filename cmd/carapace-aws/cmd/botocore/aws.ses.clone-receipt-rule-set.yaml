name: clone-receipt-rule-set
description: Creates a receipt rule set by cloning an existing one.
flags:
    --original-rule-set-name=!: The name of the rule set to clone.
    --rule-set-name=!: The name of the rule set to create.
