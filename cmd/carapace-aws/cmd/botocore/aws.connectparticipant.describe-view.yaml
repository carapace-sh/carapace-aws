name: describe-view
description: Retrieves the view for the specified view token.
flags:
    --connection-token=!: The connection token.
    --view-token=!: An encrypted token originating from the interactive message of a ShowView block operation.
