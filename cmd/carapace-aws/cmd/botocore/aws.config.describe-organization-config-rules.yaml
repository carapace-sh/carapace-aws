name: describe-organization-config-rules
description: Returns a list of organization Config rules.
flags:
    --limit=: The maximum number of organization Config rules returned on each page.
    --next-token=: The `nextToken` string returned on a previous page that you use to get the next page of results in a paginated response.
    --organization-config-rule-names=: The names of organization Config rules for which you want details.
