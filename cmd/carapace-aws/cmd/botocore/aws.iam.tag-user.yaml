name: tag-user
description: Adds one or more tags to an IAM user.
flags:
    --tags=!: The list of tags that you want to attach to the IAM user.
    --user-name=!: The name of the IAM user to which you want to add tags.
