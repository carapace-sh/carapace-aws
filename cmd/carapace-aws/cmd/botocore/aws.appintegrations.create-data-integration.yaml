name: create-data-integration
description: Creates and persists a DataIntegration resource.
flags:
    --client-token=: A unique, case-sensitive identifier that you provide to ensure the idempotency of the request.
    --description=: A description of the DataIntegration.
    --file-configuration=: The configuration for what files should be pulled from the source.
    --kms-key=!: The KMS key ARN for the DataIntegration.
    --name=!: The name of the DataIntegration.
    --object-configuration=: The configuration for what data should be pulled from the source.
    --schedule-config=: The name of the data and how often it should be pulled from the source.
    --source-uri=: The URI of the data source.
    --tags=: The tags used to organize, track, or control access for this resource.
