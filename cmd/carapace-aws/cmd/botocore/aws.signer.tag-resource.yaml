name: tag-resource
description: Adds one or more tags to a signing profile.
flags:
    --resource-arn=!: The Amazon Resource Name (ARN) for the signing profile.
    --tags=!: One or more tags to be associated with the signing profile.
