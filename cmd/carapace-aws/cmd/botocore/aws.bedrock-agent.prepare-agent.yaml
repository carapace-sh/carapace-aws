name: prepare-agent
description: Creates a `DRAFT` version of the agent that can be used for internal testing.
flags:
    --agent-id=!: The unique identifier of the agent for which to create a `DRAFT` version.
