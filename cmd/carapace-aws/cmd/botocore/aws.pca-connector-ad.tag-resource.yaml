name: tag-resource
description: Adds one or more tags to your resource.
flags:
    --resource-arn=!: The Amazon Resource Name (ARN) that was returned when you created the resource.
    --tags=!: Metadata assigned to a directory registration consisting of a key-value pair.
