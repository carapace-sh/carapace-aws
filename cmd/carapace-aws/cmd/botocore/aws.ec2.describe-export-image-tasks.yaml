name: describe-export-image-tasks
description: Describes the specified export image tasks or all of your export image tasks.
flags:
    --dry-run: Checks whether you have the required permissions for the action, without actually making the request, and provides an error response.
    --export-image-task-ids=: The IDs of the export image tasks.
    --filters=: 'Filter tasks using the `task-state` filter and one of the following values: `active`, `completed`, `deleting`, or `deleted`.'
    --max-results=: The maximum number of results to return in a single call.
    --next-token=: A token that indicates the next page of results.
    --no-dry-run: Checks whether you have the required permissions for the action, without actually making the request, and provides an error response.
