name: describe-addresses
description: Returns a specified number of `ADDRESS` objects.
flags:
    --max-results=: The number of `ADDRESS` objects to return.
    --next-token=: HTTP requests are stateless.
