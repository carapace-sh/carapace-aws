name: create-model-bias-job-definition
description: Creates the definition for a model bias job.
flags:
    --cli-input-json=: Read arguments from the JSON string provided
    --cli-input-yaml=: Read arguments from the YAML string provided
    --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request
    --job-definition-name=!: The name of the bias job definition.
    --job-resources=!: ""
    --model-bias-app-specification=!: Configures the model bias job to run a specified Docker container image.
    --model-bias-baseline-config=: The baseline configuration for a model bias job.
    --model-bias-job-input=!: Inputs for the model bias job.
    --model-bias-job-output-config=!: ""
    --network-config=: Networking options for a model bias job.
    --role-arn=!: The Amazon Resource Name (ARN) of an IAM role that Amazon SageMaker AI can assume to perform tasks on your behalf.
    --stopping-condition=: ""
    --tags=: (Optional) An array of key-value pairs.
