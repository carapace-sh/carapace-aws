name: list-rule-types
description: Lists the rules for the condition.
flags:
    --region-filter=: The rule Region to filter on.
    --rule-owner-filter=: The rule owner to filter on.
completion:
    flag:
        rule-owner-filter:
            - AWS
