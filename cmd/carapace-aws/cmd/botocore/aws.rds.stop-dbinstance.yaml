name: stop-dbinstance
description: Stops an Amazon RDS DB instance temporarily.
flags:
    --dbinstance-identifier=!: The user-supplied instance identifier.
    --dbsnapshot-identifier=: The user-supplied instance identifier of the DB Snapshot created immediately before the DB instance is stopped.
