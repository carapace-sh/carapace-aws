name: list-members
description: List members associated with the Amazon Inspector delegated administrator for your organization.
flags:
    --max-results=: The maximum number of results the response can return.
    --next-token=: A token to use for paginating results that are returned in the response.
    --no-only-associated: Specifies whether to list only currently associated members if `True` or to list all members within the organization if `False`.
    --only-associated: Specifies whether to list only currently associated members if `True` or to list all members within the organization if `False`.
