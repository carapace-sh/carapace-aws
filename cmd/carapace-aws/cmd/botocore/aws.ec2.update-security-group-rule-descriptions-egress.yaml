name: update-security-group-rule-descriptions-egress
description: Updates the description of an egress (outbound) security group rule.
flags:
    --dry-run: Checks whether you have the required permissions for the action, without actually making the request, and provides an error response.
    --group-id=: The ID of the security group.
    --group-name=: \[Default VPC] The name of the security group.
    --ip-permissions=: The IP permissions for the security group rule.
    --no-dry-run: Checks whether you have the required permissions for the action, without actually making the request, and provides an error response.
    --security-group-rule-descriptions=: The description for the egress security group rules.
