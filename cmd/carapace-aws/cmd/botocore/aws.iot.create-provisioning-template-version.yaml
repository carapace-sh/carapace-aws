name: create-provisioning-template-version
description: Creates a new version of a provisioning template.
flags:
    --cli-input-json=: Read arguments from the JSON string provided
    --cli-input-yaml=: Read arguments from the YAML string provided
    --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request
    --set-as-default=: Sets a fleet provision template version as the default version.
    --template-body=!: The JSON formatted contents of the provisioning template.
    --template-name=!: The name of the provisioning template.
