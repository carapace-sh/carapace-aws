name: update-streaming-distribution
description: Update a streaming distribution.
flags:
    --id=!: The streaming distribution's id.
    --if-match=: The value of the `ETag` header that you received when retrieving the streaming distribution's configuration.
    --streaming-distribution-config=!: The streaming distribution's configuration information.
