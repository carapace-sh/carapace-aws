name: create-package
description: Create a package for use with Amazon ES domains.
flags:
    --cli-input-json=: Read arguments from the JSON string provided
    --cli-input-yaml=: Read arguments from the YAML string provided
    --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request
    --package-description=: Description of the package.
    --package-name=!: Unique identifier for the package.
    --package-source=!: The customer S3 location `PackageSource` for importing the package.
    --package-type=!: Type of package.
completion:
    flag:
        package-type:
            - TXT-DICTIONARY
