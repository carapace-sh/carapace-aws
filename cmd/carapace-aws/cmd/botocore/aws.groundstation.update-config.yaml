name: update-config
description: Updates the `Config` used when scheduling contacts.
flags:
    --config-data=!: Parameters of a `Config`.
    --config-id=!: UUID of a `Config`.
    --config-type=!: Type of a `Config`.
    --name=!: Name of a `Config`.
completion:
    flag:
        config-type:
            - antenna-downlink
            - antenna-downlink-demod-decode
            - tracking
            - dataflow-endpoint
            - antenna-uplink
            - uplink-echo
            - s3-recording
