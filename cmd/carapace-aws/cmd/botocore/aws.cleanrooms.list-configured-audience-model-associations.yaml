name: list-configured-audience-model-associations
description: Lists information about requested configured audience model associations.
flags:
    --max-results=: The maximum number of results that are returned for an API request call.
    --membership-identifier=!: A unique identifier for a membership that contains the configured audience model associations that you want to retrieve.
    --next-token=: The pagination token that's used to fetch the next set of results.
