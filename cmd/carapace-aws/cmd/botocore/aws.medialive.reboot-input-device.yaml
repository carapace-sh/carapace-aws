name: reboot-input-device
description: Send a reboot command to the specified input device.
flags:
    --force=: Force a reboot of an input device.
    --input-device-id=!: The unique ID of the input device to reboot.
completion:
    flag:
        force:
            - "NO"
            - "YES"
