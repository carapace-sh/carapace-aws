name: get-user-pool-mfa-config
description: Given a user pool ID, returns configuration for sign-in with WebAuthn authenticators and for multi-factor authentication (MFA).
flags:
    --user-pool-id=!: The ID of the user pool where you want to query WebAuthn and MFA configuration.
