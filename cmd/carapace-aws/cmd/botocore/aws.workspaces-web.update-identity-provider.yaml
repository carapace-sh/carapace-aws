name: update-identity-provider
description: Updates the identity provider.
flags:
    --client-token=: A unique, case-sensitive identifier that you provide to ensure the idempotency of the request.
    --identity-provider-arn=!: The ARN of the identity provider.
    --identity-provider-details=: The details of the identity provider.
    --identity-provider-name=: The name of the identity provider.
    --identity-provider-type=: The type of the identity provider.
completion:
    flag:
        identity-provider-type:
            - SAML
            - Facebook
            - Google
            - LoginWithAmazon
            - SignInWithApple
            - OIDC
