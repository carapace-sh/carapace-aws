name: create-image
description: Creates an Amazon EBS-backed AMI from an Amazon EBS-backed instance that is either running or stopped.
flags:
    --block-device-mappings=: The block device mappings.
    --cli-input-json=: Read arguments from the JSON string provided.
    --cli-input-yaml=: Read arguments from the YAML string provided.
    --description=: A description for the new image.
    --dry-run: Checks whether you have the required permissions for the action, without actually making the request, and provides an error response.
    --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
    --instance-id=!: The ID of the instance.
    --name=!: A name for the new image.
    --no-dry-run: Checks whether you have the required permissions for the action, without actually making the request, and provides an error response.
    --no-no-reboot: Indicates whether or not the instance should be automatically rebooted before creating the image.
    --no-reboot: Indicates whether or not the instance should be automatically rebooted before creating the image.
    --snapshot-location=: Only supported for instances in Local Zones.
    --tag-specifications=: The tags to apply to the AMI and snapshots on creation.
completion:
    flag:
        snapshot-location:
            - regional
            - local
