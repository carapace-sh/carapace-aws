name: create-datastore
description: Create a data store.
flags:
    --cli-input-json=: Read arguments from the JSON string provided.
    --cli-input-yaml=: Read arguments from the YAML string provided.
    --client-token=!: A unique identifier for API idempotency.
    --datastore-name=: The data store name.
    --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
    --kms-key-arn=: The Amazon Resource Name (ARN) assigned to the Key Management Service (KMS) key for accessing encrypted data.
    --lambda-authorizer-arn=: The ARN of the authorizer's Lambda function.
    --lossless-storage-format=: The lossless storage format for the datastore.
    --tags=: The tags provided when creating a data store.
completion:
    flag:
        lossless-storage-format:
            - HTJ2K
            - JPEG_2000_LOSSLESS
