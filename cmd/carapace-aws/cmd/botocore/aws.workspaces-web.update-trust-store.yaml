name: update-trust-store
description: Updates the trust store.
flags:
    --certificates-to-add=: A list of CA certificates to add to the trust store.
    --certificates-to-delete=: A list of CA certificates to delete from a trust store.
    --client-token=: A unique, case-sensitive identifier that you provide to ensure the idempotency of the request.
    --trust-store-arn=!: The ARN of the trust store.
