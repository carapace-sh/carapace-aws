name: manage-sparql-statistics
description: Manages the generation and use of RDF graph statistics.
flags:
    --mode=: The statistics generation mode.
completion:
    flag:
        mode:
            - disableAutoCompute
            - enableAutoCompute
            - refresh
