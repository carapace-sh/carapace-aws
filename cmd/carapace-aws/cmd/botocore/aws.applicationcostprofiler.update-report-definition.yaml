name: update-report-definition
description: Updates existing report in AWS Application Cost Profiler.
flags:
    --destination-s3-location=!: Required.
    --format=!: Required.
    --report-description=!: Required.
    --report-frequency=!: Required.
    --report-id=!: Required.
completion:
    flag:
        format:
            - CSV
            - PARQUET
        report-frequency:
            - MONTHLY
            - DAILY
            - ALL
