name: create-endpoint
description: Creates an endpoint using the endpoint configuration specified in the request.
flags:
    --deployment-config=: ""
    --endpoint-config-name=!: The name of an endpoint configuration.
    --endpoint-name=!: The name of the endpoint.The name must be unique within an Amazon Web Services Region in your Amazon Web Services account.
    --tags=: An array of key-value pairs.
