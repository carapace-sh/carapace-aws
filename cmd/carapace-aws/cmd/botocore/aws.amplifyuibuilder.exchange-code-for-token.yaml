name: exchange-code-for-token
description: This is for internal use.
flags:
    --provider=!: The third-party provider for the token.
    --request=!: Describes the configuration of the request.
completion:
    flag:
        provider:
            - figma
