name: list-identity-sources
description: Returns a paginated list of all of the identity sources defined in the specified policy store.
flags:
    --filters=: Specifies characteristics of an identity source that you can use to limit the output to matching identity sources.
    --max-results=: Specifies the total number of results that you want included in each response.
    --next-token=: Specifies that you want to receive the next page of results.
    --policy-store-id=!: Specifies the ID of the policy store that contains the identity sources that you want to list.
