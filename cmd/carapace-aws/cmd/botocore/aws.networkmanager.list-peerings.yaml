name: list-peerings
description: Lists the peerings for a core network.
flags:
    --cli-input-json=: Read arguments from the JSON string provided
    --cli-input-yaml=: Read arguments from the YAML string provided
    --core-network-id=: The ID of a core network.
    --edge-location=: Returns a list edge locations for the
    --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request
    --max-results=: The maximum number of results to return.
    --next-token=: The token for the next page of results.
    --peering-type=: Returns a list of a peering requests.
    --state=: Returns a list of the peering request states.
completion:
    flag:
        peering-type:
            - TRANSIT_GATEWAY
        state:
            - CREATING
            - FAILED
            - AVAILABLE
            - DELETING
