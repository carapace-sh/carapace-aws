name: list-tags
description: List all of the tags for a DAX cluster.
flags:
    --next-token=: An optional token returned from a prior request.
    --resource-name=!: The name of the DAX resource to which the tags belong.
