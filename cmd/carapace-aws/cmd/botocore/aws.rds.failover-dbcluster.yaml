name: failover-dbcluster
description: Forces a failover for a DB cluster.
flags:
    --dbcluster-identifier=!: The identifier of the DB cluster to force a failover for.
    --target-dbinstance-identifier=: The name of the DB instance to promote to the primary DB instance.
