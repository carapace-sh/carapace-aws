name: describe-metadata-model-assessments
description: Returns a paginated list of metadata model assessments for your account in the current region.
flags:
    --filters=: Filters applied to the metadata model assessments described in the form of key-value pairs.
    --marker=: Specifies the unique pagination token that makes it possible to display the next page of results.
    --max-records=: The maximum number of records to include in the response.
    --migration-project-identifier=!: The name or Amazon Resource Name (ARN) of the migration project.
