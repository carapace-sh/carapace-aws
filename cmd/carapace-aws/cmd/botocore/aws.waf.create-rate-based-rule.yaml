name: create-rate-based-rule
description: This is **AWS WAF Classic** documentation.
flags:
    --change-token=!: The `ChangeToken` that you used to submit the `CreateRateBasedRule` request.
    --cli-input-json=: Read arguments from the JSON string provided.
    --cli-input-yaml=: Read arguments from the YAML string provided.
    --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
    --metric-name=!: A friendly name or description for the metrics for this `RateBasedRule`.
    --name=!: A friendly name or description of the [RateBasedRule]().
    --rate-key=!: The field that AWS WAF uses to determine if requests are likely arriving from a single source and thus subject to rate monitoring.
    --rate-limit=!: The maximum number of requests, which have an identical value in the field that is specified by `RateKey`, allowed in a five-minute period.
    --tags=: ""
completion:
    flag:
        rate-key:
            - IP
