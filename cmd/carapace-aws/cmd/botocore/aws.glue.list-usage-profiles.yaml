name: list-usage-profiles
description: List all the Glue usage profiles.
flags:
    --cli-input-json=: Read arguments from the JSON string provided
    --cli-input-yaml=: Read arguments from the YAML string provided
    --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request
    --max-results=: The maximum number of usage profiles to return in a single response.
    --next-token=: A continuation token, included if this is a continuation call.
