name: create-domain
description: Creates a domain.
flags:
    --domain=!: The name of the domain to create.
    --encryption-key=: The encryption key for the domain.
    --tags=: One or more tag key-value pairs for the domain.
