name: create-keys-and-certificate
description: Creates a 2048-bit RSA key pair and issues an X.509 certificate using the issued public key.
flags:
    --set-as-active=: Specifies whether the certificate is active.
