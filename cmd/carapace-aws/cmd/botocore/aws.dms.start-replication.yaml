name: start-replication
description: For a given DMS Serverless replication configuration, DMS connects to the source endpoint and collects the metadata to analyze the replication workload.
flags:
    --cdc-start-position=: Indicates when you want a change data capture (CDC) operation to start.
    --cdc-start-time=: Indicates the start time for a change data capture (CDC) operation.
    --cdc-stop-position=: Indicates when you want a change data capture (CDC) operation to stop.
    --cli-input-json=: Read arguments from the JSON string provided.
    --cli-input-yaml=: Read arguments from the YAML string provided.
    --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
    --premigration-assessment-settings=: User-defined settings for the premigration assessment.
    --replication-config-arn=!: The Amazon Resource Name of the replication for which to start replication.
    --start-replication-type=!: The replication type.
