name: create-schema
description: Creates an Amazon Personalize schema from the specified schema string.
flags:
    --domain=: The domain for the schema.
    --name=!: The name for the schema.
    --schema=!: A schema in Avro JSON format.
completion:
    flag:
        domain:
            - ECOMMERCE
            - VIDEO_ON_DEMAND
