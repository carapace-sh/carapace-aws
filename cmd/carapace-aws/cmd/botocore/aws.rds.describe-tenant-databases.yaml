name: describe-tenant-databases
description: Describes the tenant databases in a DB instance that uses the multi-tenant configuration.
flags:
    --dbinstance-identifier=: The user-supplied DB instance identifier, which must match the identifier of an existing instance owned by the Amazon Web Services account.
    --filters=: A filter that specifies one or more database tenants to describe.
    --marker=: An optional pagination token provided by a previous `DescribeTenantDatabases` request.
    --max-records=: The maximum number of records to include in the response.
    --tenant-dbname=: The user-supplied tenant database name, which must match the name of an existing tenant database on the specified DB instance owned by your Amazon Web Services account.
