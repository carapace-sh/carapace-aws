name: create-routing-rule
description: Creates a RoutingRule.
flags:
    --actions=!: Represents a routing rule action.
    --conditions=!: Represents a condition.
    --domain-name-id=: The domain name ID.
    --domain-name=!: The domain name.
    --priority=!: Represents the priority of the routing rule.
