name: create-account-pool
description: Creates an account pool.
flags:
    --account-source=!: The source of accounts for the account pool.
    --cli-input-json=: Read arguments from the JSON string provided.
    --cli-input-yaml=: Read arguments from the YAML string provided.
    --description=: The description of the account pool.
    --domain-identifier=!: The ID of the domain where the account pool is created.
    --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request.
    --name=!: The name of the account pool.
    --resolution-strategy=!: The mechanism used to resolve the account selection from the account pool.
completion:
    flag:
        resolution-strategy:
            - MANUAL
