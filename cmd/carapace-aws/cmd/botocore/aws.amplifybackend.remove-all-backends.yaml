name: remove-all-backends
description: Removes all backend environments from your Amplify project.
flags:
    --app-id=!: The app ID.
    --clean-amplify-app=: Cleans up the Amplify Console app if this value is set to true.
