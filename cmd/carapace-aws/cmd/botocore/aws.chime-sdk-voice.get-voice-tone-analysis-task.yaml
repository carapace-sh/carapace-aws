name: get-voice-tone-analysis-task
description: Retrieves the details of a voice tone analysis task.
flags:
    --cli-input-json=: Read arguments from the JSON string provided
    --cli-input-yaml=: Read arguments from the YAML string provided
    --generate-cli-skeleton: Prints a JSON skeleton to standard output without sending an API request
    --is-caller!: Specifies whether the voice being analyzed is the caller (originator) or the callee (responder).
    --no-is-caller!: Specifies whether the voice being analyzed is the caller (originator) or the callee (responder).
    --voice-connector-id=!: The Voice Connector ID.
    --voice-tone-analysis-task-id=!: The ID of the voice tone analysis task.
