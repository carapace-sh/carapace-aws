name: list-id-namespaces
description: Returns a list of all ID namespaces.
flags:
    --max-results=: The maximum number of `IdNamespace` objects returned per page.
    --next-token=: The pagination token from the previous API call.
