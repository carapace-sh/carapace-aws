name: describe-backup-policy
description: Returns the backup policy for the specified EFS file system.
flags:
    --file-system-id=!: Specifies which EFS file system for which to retrieve the `BackupPolicy`.
