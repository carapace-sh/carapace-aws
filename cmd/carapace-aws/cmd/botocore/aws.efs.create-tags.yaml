name: create-tags
description: DEPRECATED - `CreateTags` is deprecated and not maintained.
flags:
    --file-system-id=!: The ID of the file system whose tags you want to modify (String).
    --tags=!: An array of `Tag` objects to add.
