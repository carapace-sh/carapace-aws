name: list-compute-node-groups
description: Returns a list of all compute node groups associated with a cluster.
flags:
    --cluster-identifier=!: The name or ID of the cluster to list compute node groups for.
    --max-results=: The maximum number of results that are returned per call.
    --next-token=: The value of `nextToken` is a unique pagination token for each page of results returned.
