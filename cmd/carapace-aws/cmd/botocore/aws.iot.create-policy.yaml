name: create-policy
description: Creates an IoT policy.
flags:
    --policy-document=!: The JSON document that describes the policy.
    --policy-name=!: The policy name.
    --tags=: Metadata which can be used to manage the policy.
